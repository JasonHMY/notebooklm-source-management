# Changelog

所有对该项目的显著更改都将记录在此文件中。

## Changelog Writing Guidelines (写作规范)

- 顶部最多保留一个 `## [Unreleased] (未发布)`。所有尚未改版本号和重新打包的改动都先写在这里。
- 正式发布段标题使用 `## [YYYY-MM-DD] [x.y.z]`。日期必须使用实际发布/打包当天的本地日期，并与 `manifest.json`、`package.json`、README 版本徽章和 release zip 版本保持一致。
- 每个发布段只保留实际需要的分类，分类名必须精确使用 `### Added`、`### Changed`、`### Fixed`、`### Security`、`### Removed`。不要在分类标题后追加中文括号，也不要创建空分类。
- 分类顺序固定为 `Added`、`Changed`、`Fixed`、`Security`、`Removed`；某个分类没有内容时直接省略。
- 条目格式统一为 `- **中文标题 (English Title)**: 具体说明。` 标题应短而明确，正文说明用户可感知的影响、修复范围或必要的技术原因。
- 实现细节较多或较长的条目，正文开头用 `**影响**: ` 加一句用户视角的影响摘要（用户能直接感知什么）；纯内部改动（重构、测试、文档、无可感知的性能优化）统一写「对扩展使用无可感知变化」。标题已自解释的短条目可省略此摘要。
- 只记录可验证的功能、修复、安全加固、打包和兼容性变化。避免夸张营销语、泛泛的“优化体验/提升稳定性”，除非同时写清楚具体修复点。
- 同一类相关改动应合并成一条高信号记录；不同风险面或不同用户影响的改动应拆开写，便于回溯。
- Agent 写入流程：先读本规范和 `AGENTS.md`；非发布改动写入 `Unreleased`；如果同步改了项目结构、功能域、存储 key、测试入口、发布流程或维护规则，同时更新 `docs/PROJECT_DIRECTORY.md`；发布前再把 `Unreleased` 内容移动到正式版本段。
- 每次发布前至少确认 changelog 顶部版本、项目版本号、release zip 文件名和 `git diff --check` 结果一致。

模板：

```md
## [Unreleased] (未发布)

### Changed
- **中文标题 (English Title)**: **影响**: 一句用户视角摘要（纯内部改动写「对扩展使用无可感知变化」）。 写清楚具体影响、范围或必要的技术原因。
- **短条目标题 (Short Entry)**: 标题已自解释的短条目，正文写清即可，可省略「影响」摘要。
```

## [Unreleased] (未发布)

### Changed
- **拖拽性能可选基准 (Opt-in Drag Performance Benchmark)**: **影响**: 对扩展使用无可感知变化。 新增 `npm run benchmark:drag`，仅在 `DRAG_BENCHMARK=1` 时运行真实扩展上下文中的 100/500 行、单项/50 项选择 reflow 拖拽样本；每个 prepare 样本会在计时外先还原真实下一次 pointerdown 后的 host/pseudo-hover 状态、完成 layout settle 并把全部 instrumentation 归零，再同时观察 `getBoundingClientRect`、`offsetHeight` 与实际 DOM mutation；manager-active callback 以 isolated-world logical rAF ID 精确绑定每次 dragover，只汇总 20 次同步 prepare 与 50 个目标 callback 的三类 DOM 调用增量。默认 smoke/CI 不执行该 timing 基准，基线文档按同一修正版 harness 重建 Before，After 与最终 10% 验收仍留给后续热路径任务。
- **记录存储加固完成证据 (Record Storage Hardening Completion Evidence)**: **影响**: 对扩展使用无可感知变化。 优化路线图基线报告现记录 Storage Integrity 工作流的最终已验证提交 `39bdfa04b49cd939be8d20f66adc16c4d607a66f`、lint/重点 Jest/unit/headless smoke/package 与安全/范围检查结果，以及最终复核结论；拖拽和架构工作流仍保持未执行状态。
- **集中存储契约定义 (Centralize Storage Contract Definitions)**: **影响**: 对扩展使用无可感知变化。 新增纯共享 `storage-contract` 模块，统一 schema/import 版本、状态/历史/recovery/开发日志 key builders、精确 notebook key ownership 与 schema compatibility；content、background、manifest 与测试 loader 现使用同一契约，既有存储键、导入格式、权限和数据行为不变。
- **产品更名为 GeminiNotebook-Source-Management (Rename Product to GeminiNotebook-Source-Management)**: **影响**: 扩展名称、启动器提示、欢迎页和三语平台文案统一使用新品牌与 Gemini Notebook 名称；npm 包名、发布 zip 及设置页导出的 JSON 文件名前缀同步改为 `gemininotebook-source-management`。 现有 `notebooklm.google.com` 入口、Chrome Web Store / GitHub 链接、`notebooklm-source-management-config` 备份格式、存储键和 `NSM_*` 运行时标识保持不变，已有数据与旧备份无需迁移。
- **新增优化加固实施路线图 (Add Optimization Hardening Implementation Roadmap)**: **影响**: 对扩展使用无可感知变化。 新增一份总路线图和三份专项计划，按数据完整性、拖拽正确性与性能、架构与无障碍的顺序，把已确认问题落实到具体 Interface、文件、失败测试、验证命令、提交和回滚门；本次仅新增计划文档，未修改 runtime、存储、权限或拖拽行为。
- **记录优化加固基线 (Record Optimization Hardening Baseline)**: **影响**: 对扩展使用无可感知变化。 记录路线图开始前的 lint、unit 和默认 headless smoke 通过结果及起始 commit，为后续存储、拖拽和架构任务提供可核对的执行台账；未修改 runtime、存储或拖拽行为。

### Fixed
- **原生无效落点反馈与 dragover 同步 (Synchronize Native Invalid-Drop Feedback)**: **影响**: 拖到自身、已选批量成员、文件夹自身/后代或列表外等无效位置时，浏览器的禁止落下反馈会在当前 `dragover` 事件内立即出现；指针随后回到有效位置也会立即恢复为可移动，不再等待下一动画帧或短暂沿用上一位置的状态。同步判断只消费当前 clean geometry snapshot，不新增 DOM 测量；snapshot 缺失或因滚动/render/hover 变化而 dirty 时先保守显示可移动，真正 drop 仍由同步 fresh flush fail closed。rAF、auto-scroll 与 drop/dragend 不再保留或异步写入 `DataTransfer`，setter 异常不会阻止 `preventDefault` 和帧调度；Classic 与避让 Beta 共用同一规则。
- **自动滚动期间持续刷新拖拽落点 (Refresh Drag Intent During Auto-Scroll)**: **影响**: 指针停在来源列表边缘触发连续自动滚动时，即使浏览器没有继续派发新的 `dragover`，插入位置也会随滚动后的列表内容更新；滚动后立即松手会先同步刷新落点再改动来源顺序，避免按上一帧位置误放。 auto-scroll controller 只在 `scrollTop` 实际变化时回传滚动结果，tree 通过统一 geometry invalidation 与现有 drag-frame rAF 合并刷新；原生 `scroll` 事件和 controller callback 无论谁先到都只记录一次精确 delta，到达边界、停止或拖拽清理后不再回调。Classic 与避让 Beta 共用同一 fail-closed drop 刷新路径，未新增权限、存储键或 host surface。
- **大列表避让拖拽按单帧快照稳定运行 (Stabilize Large-List Reflow Drag Frames with One Snapshot)**: **影响**: 避让 Beta 在 100/500 行、单项或 50 项多选时，不再因每帧重复扫描、滚动后全量重测或数百个离屏位移动画而卡顿；500 行 50 项多选的最终实测 callback p95 从 5.3 ms 降到 2.1 ms。 dragover 现按一次 geometry read → 纯 plan → 集中 write 执行，以 source/group 类型化 map 复用元素和同步回写 transform delta；根列表及嵌套列表滚动按精确 delta 修补缓存，render、尺寸变化、混合失效或无法验证的 DOM 会 fail closed 后完整重建。只有可视区及一个真实行高缓冲范围参与位移动画，离屏行保持静态 transform 并在拖拽结束统一清理；ghost 仅复制明确允许的视觉样式。官方 100/500 行四组合基准、强制布局阶段及 geometry/query 调用预算全部通过，Classic 模式、存储、权限和 host surface 不变。
- **避让拖拽按真实占位让位并完整恢复取消状态 (Measure Real Reflow Footprints and Restore Cancelled Drags)**: **影响**: 避让 Beta 在混合来源/文件夹、折叠边距、content-box/border-box、固定高度容器与跨容器多选时，会按真实折叠位移打开完整空槽；列表滚到接近底部后开始拖拽、按 Esc 取消或拖到列表外，也不会再出现滚动跳位、占位过冲或残留折叠/动画。 dragstart 使用一次批量终态探针和最多三次强制布局阶段，分别测量 direct-host 与 root outer 位移，恢复原 inline 值、`!important` 优先级、类状态及各 host 的 `scrollTop` 后才交还原生拖拽；有效的连续拖拽复用 host 激活态，无效/编辑控件起点会清理该状态。Classic 模式、存储、权限与 host surface 不变。
- **每个笔记本加载均恢复经典拖拽布局约束 (Enforce Classic Placement on Every Notebook Load)**: **影响**: 使用经典拖拽模式时，切换笔记本、延迟恢复来源状态或重新挂载来源面板后，夹在文件夹之间的根层级来源都会一致移回底部「未分组」区域，不再只在手动切换模式时修正。 修正前会先确认完整偏好与当前笔记本实例、保存迁移前历史快照，并在紧邻 mutation 前再次核对 state 引用；偏好读取或 checkpoint 失败时保持原状态不变，迁移使用不写入撤销栈的 immediate critical save，失败则保留 recovery。设置切换只有在 Classic 约束完成后才报告成功，失败时尝试恢复原拖拽模式且开关显示始终跟随实际模式；偏好请求按单调顺序处理，显式成功的新保存会淘汰更早的待处理读取（不完整响应仍不会把偏好标为已验证），失败保存则不会丢弃更早的有效读取，避免 Classic 保存完成后被旧 Reflow 响应覆盖。来源面板重新挂载会先执行同一约束，成功迁移复用唯一一次保存，只有无需迁移时才执行原有 reattach 保存，并在该额外保存前再次核对 notebook 与 manager instance，避免旧面板 continuation 写入新笔记本。
- **过滤视图拖拽锚点对齐 (Align Filtered Drag Slots to Visible Anchors)**: **影响**: 搜索或快速视图隐藏部分来源时，拖到最后一个可见来源或文件夹之后会紧跟该可见锚点落入底层顺序，不再在根层级与文件夹内分别落到不同位置；可见列表为空但底层仍有条目时不再猜测落点。 单源、多源与文件夹排序统一按 `type + key/id` 解析可见锚点，底层空列表仍接受索引 `0`；同容器拖拽的移除后索引修正继续留在实际 mutation 阶段。
- **存储读取兼容性与队列一致性 (Harden Storage Load Compatibility and FIFO Consistency)**: **影响**: 刚完成保存、关闭管理器或立即重建时会等待同 notebook 后台保存完成再读取；未来版本或非法 primary/backup 不再因当前版本看起来“为空”而被旧备份掩盖并覆盖。 `LOAD_STATE` 现一次返回原始 primary、backup 与 history，按 `_saveRevision`、`_savedAt` 先确定权威候选，runtime 不可用时才走只读本地回退；未新增 storage key、权限、依赖或 host surface。
- **导入事务与生命周期隔离 (Isolate Import Transactions from Lifecycle and Navigation)**: **影响**: 导入尚未确认、被拒绝或确认结果不明确时，页面隐藏/关闭不会再覆盖恢复点或稍后保存被拒绝的导入状态；导入期间切换 notebook 也不会在新 notebook 回滚、重绘或显示旧 notebook 提示。 `import_pending`、`import_ack_unknown`、`import_rollback_required` 由导入事务独占，并按起始 project id 与 manager instance token 约束异步完成。
- **开发者日志精确隔离并避免并发丢失 (Scope and Serialize Developer Logs)**: **影响**: 同一笔记本连续记录、清空或立即读取开发者日志时，不再因并发读写丢失条目或读到旧结果；不同笔记本的日志操作仍可并行。 后台现按完整 `sourcesPlusDeveloperLogs_<projectId>` 精确绑定发送方笔记本，拒绝跨笔记本、共享前缀、额外后缀及裸 `/notebook/` URL，并在访问 storage 前失败关闭；每个日志 key 的 append/clear 共用 FIFO，load 等待同 key 的待处理任务，原有 500 条、512 KiB 和脱敏约束保持不变。
- **配额裁剪保留手动恢复点 (Preserve Manual Restore Points During Quota Trimming)**: **影响**: 存储空间接近上限时，自动清理仍保留所有命名恢复点和最新自动快照；若这些受保护内容仍无法写入，扩展会明确拒绝增长写入而不会删除手动恢复点。
- **同笔记本读取等待保存完成 (Serialize State Loads Behind Saves)**: **影响**: 刚整理并保存来源后立即重新加载时，不再偶尔读回保存前的旧状态。 同一 notebook 的 `LOAD_STATE` 现在复用既有 save FIFO，等待 pending `SAVE_STATE` settle 后才读取 primary/backup；不同 notebook 仍可并行加载，既有 sender/key 校验、revision guard 与恢复逻辑不变。
- **不支持的存储与导入版本只读保护 (Reject Unsupported Storage and Import Versions)**: **影响**: 当本地状态来自更新版本或带有无效 schema 时，扩展不再把它当旧数据降级覆盖；未知或不完整的导入 envelope 也会直接拒绝。 权威 primary/backup 状态在迁移、历史修复和应用前先校验版本，失败时以 `unsupported_schema` 阻断当前笔记本加载实例的普通及生命周期写入；保存队列在任务开始与结果回收时复核 load scope，旧 scope 的排队任务不再发送、已在途结果也不能覆盖只读错误或推进 revision/recovery 状态。切换笔记本或新建 manager 实例后只清除该 schema 专属错误并恢复保存，旧版 bare-state 导入与 schema v1–v5 迁移保持兼容。
- **配置导入失败原子回滚 (Atomic Rollback for Failed Config Imports)**: **影响**: 配置导入在后台保存失败、抛错、延迟应用或收不到明确确认时，不再把未持久化的导入结果留在当前界面；分组、来源状态、标签、顺序与自定义高度会恢复到导入前。 导入请求从入队起保存 notebook 绑定的导入前 recovery，禁止本地 fallback，只有后台明确返回 `success: true` 才清除；空或格式不完整的响应标记为 `import_ack_unknown` 并保留显式恢复入口。保存 revision、recovery 与状态回调同时按 notebook key 和 manager instance 隔离，旧笔记本的异步结果不会覆盖新笔记本状态。
- **生命周期与关闭前保存不再丢失 (Preserve Saves Across Lifecycle Teardown)**: **影响**: 页面隐藏、关闭来源管理器、切换笔记本或进入来源详情时，尚在 debounce 中的整理结果会先进入后台保存队列，再立即关闭界面，不再因 cleanup 取消而丢失；后台暂时不可用时保留恢复快照且不覆盖主存储。 `visibilitychange:hidden` / `pagehide` 与普通保存共用 per-key FIFO，生命周期保存禁止 local primary fallback；本地应急路径同时拒绝同 revision 不同内容的覆盖，重复的相同快照仍保持幂等。已有的失败导入确认或回滚 recovery 优先级更高，后续 lifecycle 成功或失败均不会覆盖或清除该快照。

## [2026-06-16] [26.6.16]

### Changed
- **规范化「设置」面板布局 (Standardize the Settings Panel Layout)**: **影响**: 「外观自定义」「开发者功能」里的选项改成和「偏好设置」一样的整齐行(左边名称+说明、右边一个滑动开关),原生方框勾选换成与文件夹启用开关一致的滑动开关;分区顺序调整为 偏好设置 → 外观自定义 → 备份与恢复 → 帮助与反馈 →(出问题时)源指纹修复 → 开发者功能;开发者按钮分成「日志」「测试工具」两组。所有开关/按钮的功能与原来完全一致。 纯 UI 重构:`createAppearanceSettingsSection` / `createDeveloperSettingsSection` 改为统一的 `.sp-settings-preference-row`(控件移出 `.sp-settings-section-header`),三个开关(悬浮光晕 / 避让拖拽 Beta / 开发者模式)从原生 checkbox 包成 `.sp-toggle-switch`(input 保留原类名 + 追加 `sp-group-toggle-checkbox`,`change` 绑定不变),新增 `.sp-settings-preference-row > .sp-toggle-switch` 右对齐规则;`renderSettingsModal` 调整 append 顺序;`createSettingsSubsection` 提升到工厂作用域供开发者区复用,开发者 5 按钮拆成「日志」(复制/下载/清空)+「测试工具」(测试欢迎/测试更新介绍)两个带小标题子分区(新增 `ui_settings_developer_logs_title` / `ui_settings_developer_test_tools_title` 三语 key)。无功能 / 状态 / 存储 / 消息协议变更;新增 5 个结构单测,现有外观/开发者单测因 input 类名 + change 绑定保留而不变。
- **设置项调整:成功不再弹提示,失败提示抬到弹窗之上 (Settings Tweaks: Hide the Success Toast, Lift the Failure Toast Above the Modal)**: **影响**: 在设置面板打开时调整设置(悬浮光晕 / 避让拖拽 / 开发者模式开关、清空开发者日志、历史保留上限),成功时不再弹底部小提示(此前它被设置弹窗的毛玻璃盖住、看不清、纯噪音);失败时仍会提示,且被抬到毛玻璃之上、清晰可读。 toast 层(`z-index: 9999`)本就在设置 backdrop(`10000` + `blur(4px)`)与 modal(`10001` + `blur(24px)`)之下,故弹窗打开时 toast 被模糊遮挡。新增工厂内 `isSettingsModalOpen()`(`querySelector('#sp-settings-modal')`) + `announceSettingsResult(messageKey, variant)`:`variant==='success'` 且设置弹窗打开时直接 return(不弹);失败一律弹,设置打开时带 `{ elevated: true }`。`normalizeToastOptions` 透传 `elevated`,`content-toast.js` 据此给 toast 加 `.sp-toast-elevated`(`z-index: 10003`,高于 modal/backdrop/action-menu)。仅设置项调整 toast 走此路径;开发者密码闸门失败仍按原样普通提示(非设置项调整、不动其安全测试)。新增 2 个行为单测(成功被抑制、失败带 elevated)。

## [2026-06-14] [26.6.14]

### Added
- **可选择拖拽模式:经典蓝线 / 避让 Beta (Choose Your Drag Mode: Classic Blue-Line / Reflow Beta)**: **影响**: 现在可在「设置 → 外观自定义」选择拖拽手感——默认「经典」(拖动时显示蓝色插入线,与 26.5.26 一致);可开启「避让(Beta)」(拖动时其它来源避让让位、并能把来源插到文件夹之间)。更新后默认经典;「更新介绍」弹窗里有「启用避让拖拽(Beta)」按钮可一键切换,不点则保持经典。 新增顶层偏好 `dragMode`(`classic` 默认 / `reflow`,`normalizeDragMode` 校验、非法回退 classic),单引擎按模式分支:经典走蓝线反馈(`.drag-over-top/.drag-over-bottom`,从 26.5.26 恢复)+ 散源落到底部「未分组」桶(`computeDropIntent` 闸门把根层级 source 落点降级回桶,不产生定位散源)+ 无折叠/避让/落地动画;避让走现有 reflow + `state.root` 根层级定位。切回经典时把已定位散源 `sweepPositionedRootSourcesToBin` 扫回底部桶(经典无法表示定位散源)。偏好链 / 拖拽引擎 / 设置 + What's-New 全程注入 `getDragMode`/`setDragMode`。
- **来源可放到根层级任意位置(含文件夹之间,避让 Beta 模式) (Place Sources Anywhere at Root, Including Between Folders — Reflow Beta)**: **影响**: 在「避让(Beta)」拖拽模式下,可把来源拖到根层级任意位置——两个文件夹中间、文件夹之上/之下;桶为空时拖到列表底部会出现带动画的"移到未分组"落点区(经典模式下散源仍只落到底部桶)。 根层级状态模型从分离的 `state.groups`(文件夹)+ `state.ungrouped`(散源、强制排在所有文件夹之下)升级为统一的 `state.root` 异构有序数组(`{type:'group',id}|{type:'source',key}`,与 `group.children` 同构),文件夹与"已定位"散源可任意交错;`state.ungrouped` 保留为底部"未分组"桶(新导入源默认入桶、拖到底部送回桶),与"已定位"互斥。`computeDropIntent` 根落点合并进异构 `targetList=state.root`。
- **`.editorconfig` 格式护栏 (Formatting Guardrail via .editorconfig)**: **影响**: 对扩展使用无可感知变化;统一仓库各文件类型的缩进/换行约定、防新编辑漂移。 此前 JS 缩进虽统一为 4 空格,但 `content-render.js` 有零星 tab 缩进、JSON 缩进 manifest/package(2)与 `_locales/*`(4)不一致,且无任何 formatter 约束(ESLint 无 `indent` 规则)。新增 `.editorconfig`,按 glob **记录各类型的现有约定**(JS=4、`manifest/package`=2、`_locales/*`=4、CSS 按文件:`popup/styles.css`=4 / `content/styles.css`=2、md 保留尾空格;统一 LF + 末尾换行 + 去行尾空格),让新编辑不再漂移、且不在文件内制造新旧混缩。**刻意不大规模 reformat 存量**(保持 diff 外科式)。AGENTS.md「Project Constraints」补一条格式约定。`.editorconfig` 不进发布包(package.js allowlist 已确认 0 条)。CI 级强制(Prettier/@stylistic + 一次性规范化)作为可选后续,未在本次引入(避免新 dev 依赖 + 存量 churn)。

### Changed
- **避让拖拽降级为默认关闭的 Beta,默认恢复经典 (Reflow Drag Demoted to Off-by-Default Beta; Classic Restored as Default)**: **影响**: 升级到 26.6.14 后默认拖拽手感回到经典蓝线;本轮新增的避让 + 根层级任意位置插入改为需在设置或更新弹窗里主动开启的 Beta。 `dragMode` 默认 `classic`;What's-New 头号 feature row 标注 Beta、正文说明默认经典且可在此/设置开启。已在用 Beta 又切回经典的用户,其放在文件夹之间的来源会被移到底部「未分组」桶。
- **「更新介绍」页换为本轮亮点 (Refresh the What's-New Page for This Release)**: **影响**: 升级到 26.6.14 后弹出的「更新介绍」页改为展示本轮头号新功能「来源可放到根层级任意位置」+ 一条「修复与无障碍」摘要(此前仍是 26.5.29 的「拖拽优化 / 稳定性修复」)。 What's-New 弹窗的两条 feature row 文案在 en/es/zh_CN 三语同步重写:头号行(`ui_whats_new_drag_*`,icon 仍 `drag_pan`)改为根层级任意位置插入;第二行(`ui_whats_new_stability_*`,icon 仍 `verified`)改为本轮拖拽插入修复(悬停展开后避让、嵌套子文件夹抽搐、收起文件夹无反馈死区)+ 键盘/读屏支持 + 存储边界加固。仅改 message 值,键名 / icon / 模块 / 结构不变,故 What's-New 与 locales 单测无需改动。版本号比较是运行时读 manifest,故老用户升级到 26.6.14 会再次看到该页。
- **存储 schemaVersion 4 → 5(state.groups → state.root) (Storage schemaVersion 4 → 5)**: **影响**: 升级后零视觉变化;老笔记本自动迁移。 `normalizeLoadedState` 新增 v4→v5 迁移分支(`root = groups.map(id => ({type:'group',id}))`),主存储 + backup + history 快照在载入时迁移。
- **拖拽 auto-scroll 复用已解析的 sources-list 容器 (Reuse Resolved sources-list Container in Drag Auto-Scroll)**: **影响**: 对扩展使用无可感知变化;dragover 热路径去掉每帧一次重复的 shadow-root + 元素解析,纯性能重构。 `_processDragOver`(拖拽中每次 dragover 高频触发)开头已用 `getSourceListContainer()` 解析出 `#sources-list` 容器并存入 `sourceListEl`,但末尾 auto-scroll 分支又 `getShadowRoot()` + `getElementById('sources-list')` 把同一稳定节点重解析一遍。改为直接复用 `sourceListEl`(同一节点、保留同样的 null + `getBoundingClientRect` 守卫),每帧省掉 1 次 `getShadowRoot` 调用 + 1 次 `getElementById` 查询。纯行为保持重构,由 batch-drag smoke 与 content-tree 拖拽集成单测守护;lint 0/0、全量 1050 单测 + 19 smoke 全过。
- **CLAUDE.md 建统一文档地图 (Single Documentation Map in CLAUDE.md)**: **影响**: 对扩展使用无可感知变化;纯文档导航增强。 把 CLAUDE.md 的 "Authoritative Rules"(原只列 5 份)扩成覆盖全部 11 份规范文档的索引表(Doc / 真相源 / 何时读),补齐此前漏列的 `STORAGE_SCHEMA` / `MESSAGE_CONTRACTS` / `SECURITY_THREAT_MODEL` / `RELEASE_CHECKLIST` / `PRIVACY`,作为「该看哪份文档」的单一入口;并交叉引用 `nsm-discipline` skill 做按-task 路由,避免与之重复。
- **全局规范文档对齐代码:9 文件 21 处 drift 修正 (Align Global Docs With Code: 21 Fixes Across 9 Files)**: **影响**: 对扩展使用无可感知变化;纯文档 + 1 处代码注释对齐,代码行为零变更。 经 11 路并行 agent 评估 + 逐条对抗式验证(22 候选全确认、0 误报)修正:PRIVACY 把「远程 Google 字体」更正为本地打包 + `chrome-extension://` 加载(对外隐私声明纠错);PROJECT_DIRECTORY 补 `styles.css`(第三套 CSS)、CI 树补 `npm run lint`、normalizers 8→10;SECURITY 补漏记的 sender-to-key 绑定 / `__proto__` 防护 / `cssEscape` / locale web-accessible;MESSAGE_CONTRACTS 补 per-notebook key 归属校验;CLAUDE `getDropIntent`→`computeDropIntent`(含 `index.js` 注释)、helper 数 ~44→~45;STORAGE 补 sessionStorage `sourcesPlusRecovery_<id>` key + `_saveRevision`/`_savedAt` 字段、版本示例 2.7.4→26.5.29;AGENTS/RELEASE 补 `npm run lint` 验证门、删 AGENTS 内部重复条、对齐 CLAUDE↔AGENTS 引用。lint 0/0、46 套件 1050 单测全过;README / CHANGELOG 评估为干净。
- **UI_GUIDELINES 对齐代码:4 处 drift + 2 处注释时长 (Align UI_GUIDELINES With Code: 4 Doc Drifts + 2 Comment Timings)**: **影响**: 对扩展使用无可感知变化;纯文档与代码注释对齐,代码行为零变更。 经 10 路并行 agent 审计 + 逐条对抗式验证(4 候选全部确认、0 误报)修正 UI_GUIDELINES.md 与代码的不匹配:§13.1 `.sp-view-banner` 补「原生标签视图」第三用途、§13.4 ghost 堆叠更正为「顶层仅 `scale(0.95)`、层 2/3 才 offset+rotate」、§13.4 unfold 时长 200ms 更正为 `var(--sp-motion-base)`(180ms)、§23 File Map 补漏列的 `content-drag-reflow.js` / `content-drag-multi.js`(上轮补 File Map 时遗漏);同步把 `content-drag-reflow.js` 与 `content-tree-interactions.js` 各 1 处过时的「200ms」注释改为 180ms。代码行为零变更,lint 0/0、46 套件 1050 单测全过。
- **写作规范纳入「影响」摘要约定 (Codify the Impact-Summary Convention)**: **影响**: 对扩展使用无可感知变化;纯规范/文档更新。 在 CHANGELOG 顶部写作规范与 `AGENTS.md` Changelog Rules 同步新增一条约定:实现细节较多/较长的条目正文开头用 `**影响**: ` 加一句用户视角摘要,纯内部改动写「对扩展使用无可感知变化」,标题已自解释的短条目可省略;并更新规范模板示例(展示带「影响」与短条目两种)。
- **CHANGELOG 超长条目补「影响」摘要 (Add User-Impact Summary to Long CHANGELOG Entries)**: **影响**: 对扩展使用无可感知变化;纯 CHANGELOG 可读性增强。 给全文件 104 条超长(>180 字)条目正文开头补一句加粗的「影响」摘要(用户视角),原正文逐字保留(机械验证:每条新正文以原正文为后缀、标题不变);内部重构/测试/文档类统一标注「对扩展使用无可感知变化」,功能/修复/安全/无障碍类写明用户可感知差异。
- **CHANGELOG 结构修复:写作规范段移回顶部 + 合并重复分类 (Fix CHANGELOG Structure: Guidelines Back to Top + Merge Duplicate Categories)**: **影响**: 对扩展使用无可感知变化;纯 CHANGELOG 文档结构整理。 `Changelog Writing Guidelines` 段此前埋在 26.5.29 与 26.5.26 两个发布段之间,与 `AGENTS.md` / `CLAUDE.md`「位于 CHANGELOG 顶部」的契约矛盾,现移回文件顶部(标题/描述之后、首个版本段之前)。顶部 `## [Unreleased]` 补 `(未发布)` 后缀并按 `Added → Changed → Fixed → Security → Removed` 固定顺序重排;26.5.29 段内重复出现的 3 个 `### Changed`、2 个 `### Fixed` 各合并为一条并按固定顺序重排,删掉段内孤立空行。纯结构重组:全部条目正文逐字未改(以「条目行排序后与改前 diff 为空」机械验证),历史发布段(26.5.26 及更早)与规范段文字一字未动。
- **index.js 测试导出拆为 productionApi + testSurface (Split index.js's Jest Export Into productionApi + testSurface)**: **影响**: 对扩展使用无可感知变化;纯测试导出的组织性重构。 `module.exports` 块(仅 Jest 执行,Chrome 下 `module` 为 undefined、整块跳过)此前把 ~50 个生产方法镜像与 **126 个 `_*` 测试访问器**混在一个对象字面量里,真实面被测试面淹没。拆成两个命名对象:`productionApi`(模块真实方法的镜像,集成测试用)+ `testSurface`(够到内部闭包的 `_*` 访问器/setter),末尾 `module.exports = Object.assign({}, productionApi, testSurface)`。纯组织性重构、零运行时影响(该块 Chrome 不执行),导出键集合与签名完全不变,由全量单测(几十处用这些 `_` 访问器)守护。
- **index.js 架构总览注释 + 文档化三套 CSS 边界 (Architecture-Overview Docblock for index.js + Document the Three CSS Mechanisms)**: **影响**: 对扩展使用无可感知变化;只新增架构注释与文档。 `src/content/index.js`(4849 行、装配中枢)此前缺顶部总览。补一段架构 JSDoc:职责(运行时状态所有权、生命周期、消息路由、原生动作/批量、dep 装配图、测试面)、为何刻意大(共享闭包状态)、以及三套 CSS 的边界。CLAUDE.md「Shadow DOM vs global overlay」节补上第三套 `src/content/styles.css`(manifest `content_scripts[0].css` 注入、`.sources-plus-manager-active` 下 `!important` 覆写隐藏原生面板、保留在渲染树供 CDK 量算),与 `NSM_CONTENT_STYLE_TEXT`/`NSM_GLOBAL_OVERLAY_STYLE_TEXT` 区分清楚。纯注释 / 文档,零行为变更。
- **模块同步不变量补双向守护 (Bidirectional Guard for the Module-Sync Invariant)**: **影响**: 对扩展使用无可感知变化;加强测试守护、防新模块注册漏同步。 `manifest-loader-sync.test.js` 原本只校验 manifest↔loader 的 require 顺序 + 清理计数(单向),新增模块加进 loader 却漏加 harness `CONTENT_HELPER_GLOBALS` 不会被拦(「遗漏方向」)。新增 1 个断言:loader `clearContentGlobals()` 删除的 `NSM_*` 全局集合 与 harness `CONTENT_HELPER_GLOBALS` 数组**双向集合相等**。当前两者一致(各 47),以"临时删 harness 一条 → 守护变红 → 还原"验证其有效。
- **PROJECT_DIRECTORY 测试树补齐 9 个漏列单测文件 (Sync 9 Missing Unit-Test Files Into PROJECT_DIRECTORY Test Tree)**: **影响**: 对扩展使用无可感知变化;补齐项目目录里漏列的测试文件。 `docs/PROJECT_DIRECTORY.md` 第 5 节测试树漏列了 9 个实际存在的 `tests/content/*.test.js`(import-export、state-apply、undo-history、modal-tag、modal-tag-filter、modal-move、modal-command-palette、modal-welcome、modal-whats-new)—— `npm run test:unit` 本就全跑,纯 discoverability/定向运行的 doc drift。把它们补进「持久化 / history / import-export」与「欢迎 / 设置弹窗 / 标签 modal / 命令面板」两类的文件清单,使文档与现存测试文件一致。
- **spotlight 偏好关闭时跳过 pointermove 布局读写 (Skip Spotlight pointermove Layout Work When the Preference Is Off)**: **影响**: 关掉悬浮光晕后,指针移动不再做无用的布局计算,更省 CPU(视觉无变化)。 关闭悬浮光晕偏好后(`sp-appearance-no-spotlight` host class → `::before` `display:none`),`handleSpotlightPointerMove` 仍对每次指针移动跑 `getBoundingClientRect` + 两次 `setProperty`,绘制为空、纯浪费。在 handler 顶部加早返回:host 带该 class 时直接 return,跳过 resolve + 布局读 + 自定义属性写,对已关闭用户零视觉变化。新增 1 个 render 单测断言禁用时不调 `getBoundingClientRect`、返回 null。
- **getSourcePanelState 复用已抽取 identity 去重每行扫描 (Reuse Extracted Identity in getSourcePanelState's Per-Row Loop)**: **影响**: 对扩展使用无可感知变化;热路径去掉每行一次重复扫描,纯性能重构。 `getSourcePanelState` 的 `sourceElements.forEach` 每行先 `extractSourceIdentitySnapshot(row)`（约 4 次 `findElement` 多选择器查询 + token/processing/failure 信号 + 文本归一化，无任何缓存），随后又调 `hasSourceLoadingIndicator(row)` —— 后者内部就是 `Boolean(extractSourceIdentitySnapshot(row)?.hasProcessingSignal)`，等于把同一行的身份重抽一遍。`getSourcePanelState` 是热路径（`syncManagerWithPanelLifecycle` / `getManagerStatus` 等十多处调用）。改为直接读已抽取的 `identity?.hasProcessingSignal`（与相邻的 `identity?.hasFailureSignal` 对称），可证等价地省掉每行 1 次重复抽取。纯行为保持重构，由现有 `loadingRows` 系列单测（含 Material 进度指示、raw URL 临时 loading 等）守护。
- **来源视图检测按扫描 pass 记忆化 (Memoize Source-View Detection Per Scan Pass)**: **影响**: 来源很多(100-300)时面板扫描明显更省 CPU,行为不变。 `detectSourceView()` 会对来源面板跑一批 `querySelectorAll`（label 组选择器 + list/label 行扫描 + active label 控件），但在单个 `debouncedScanAndSync` 周期内被 `getSourcePanelState` → `scanAndSyncSources` → `render()` → `renderViewStateBar()` → `getNativeLabelImportPreview()` 以**完全相同的结果**触发 5-6 次，是 mutation 驱动高频路径上的主导开销（100-300 源时每 tick 数百次 `querySelectorAll`）。把 `detectSourceView` 的计算体抽成 `computeSourceView`，外层加一个**仅在扫描 pass 期间生效**的 memo（`beginSourceViewPass`/`endSourceViewPass` 包裹 `debouncedScanAndSync` 回调，`try/finally` 保证早 return / throw 也释放）：pass 内对同一 panel 只算一次、命中缓存复用；`getSourceViewInfo` 仍每次调 `updateRuntimeSourceViewInfo`，故首帧 view-transition 遥测不变。刻意**不跨 pass / 跨 tick 缓存** —— 检测结果随 NotebookLM 行流入合法变化，`endSourceViewPass` 在 depth 归零时清空缓存，每个新 pass 重新检测。pass 外（拖拽 / 视图切换等异步上下文）的 `getSourceViewInfo` 调用一律实时计算，无行为变化。新增 1 个单测锁定「同一 pass 内多次检测只扫一次、跨 pass 重新扫描、pass 外不缓存」。

### Fixed
- **悬停展开文件夹后拖入子项无法避让/插入 (Can't Insert Into a Folder Right After Hover-Expanding It Mid-Drag)**: **影响**: 把来源悬停到文件夹上等它自动展开后,立刻往里拖现在能正常避让让位、精准插到已有子项之间(此前有时插不进去)。 hover-expand 是拖拽中触发的:`executeHoverExpand` 复用点击式动画展开 `toggleGroupCollapse`,后者把 `.group-children` 临时设为 `overflow:hidden` + 固定像素高(`scrollHeight`),靠一个异步 `transitionend`(约一个 `--sp-motion-slow` 后)才还原 `height:auto`/`overflow:visible`。在那段窗口里,拖拽 reflow 给子项加的 `translateY` 让位被 `overflow:hidden` 裁切、固定高容器又长不大,于是看不到避让、底部/中间的落点槽开不出来,表现为"插不进去"(仅 hover-expand 中招——普通点击展开的文件夹在你开始拖之前 `transitionend` 早已还原,与用户"只在悬停展开时出现、且有时出现"的描述吻合)。`computeDropIntent` 的落点几何本身正确(由新单测证实),问题纯在展开期容器被钳制。修复:`executeHoverExpand` 打开文件夹后立即把 `.group-children` 设为拖拽就绪态(`height:auto`/`overflow:visible`),不再等 `transitionend`,让 reflow 从展开第一帧起就生效;点击展开的动画路径不变。新增 2 个单测:展开文件夹时光标落在两子项之间正确返回 before-source 落点;hover-expand 后容器即时为 `overflow:visible`/`height:auto`(即便预置成动画窗口的 hidden+固定高状态)。
- **拖入嵌套子文件夹时落点判定抽搐 (Drop-Intent Twitch When Dragging Into a Nested Subfolder)**: **影响**: 把来源拖进"文件夹里的子文件夹"时,落点不再在 into-group 与子项槽位之间反复抽搐。 嵌套子文件夹 S 的 `.group-container` 在落点算到"父文件夹 P 的子项中、S 之前/之处"时会被 reflow 加上 `translateY(+slotHeight)`(`computeReflow` 对 host 兄弟键位移,group-id 经 `findItemElement` 解析到外层容器)。S 的 `.group-header` / `.group-children` 自身没有 inline transform,但其 `getBoundingClientRect` 继承了容器的位移;而 `computeDropIntent` 的表头带 / 子项区带用 `unshiftedRect` 读取,它只扣除元素**自身**的 transform(对 header/children 为 0)。于是当 S 还带着上一帧 stale 位移时,本应落在"S 内部某槽位"的光标被错位下移的表头带(如 layout 170..200 被读成 210..240)误判成 into-group;下一帧 into-group 让 host 切到 S.children、`applyReflow` 的 diff 又把 S 容器位移清掉、单帧自愈 —— 但拖拽时手在那条窄缝里微动会反复重新触发,且 8px 迟滞只对 `prevIntent.kind==='into-group'` 生效、压不住这种"重新进入",表现为持续抽搐(仅嵌套触发:顶层文件夹的 id 不是任何 `group.children` 的条目,其容器不会在悬停进入它的同时被加 per-sibling 位移)。修复:`computeDropIntent` 选定 `chosenContainer` 后取 `_containerShift = extractInlineTranslateY(chosenContainer)`,把表头带与子项区带各减去它,使两条带与 Pass-2 容器带处在同一未位移坐标系、始终按真实 layout 判定。纯只读几何修正,不动 raw-rect 槽位检测 / cross-host 位移 / 8px 迟滞设计 / 任何刻意保留的护栏。新增 3 个单测(嵌套子文件夹带 stale 位移时光标过表头不再误判 into-group、同槽位结果 transform-invariant、光标在真实表头带仍正常 into-group),并扩展 `makeMockShadowList` 让容器 transform 按真实浏览器传播进后代 rect(原 mock 返回静态 rect、结构上复现不了这一祖先继承缺陷)。
- **拖拽来源经过收起文件夹时的"无反馈死区" (No-Feedback Dead Zone When Dragging a Source Over Collapsed Folders)**: **影响**: 所有来源都已归档时,把来源上拖经过收起文件夹不再「无反馈、打不开」,改为悬停渐显提示并自动展开。 当所有来源都已归入文件夹（`state.ungrouped` 为空）、把一个来源向上拖经过收起文件夹时，落点会滑进根层级 reorder 的"空-ungrouped 兜底"（`routeToNearestNeighborKind` 的 `targetList.length === 0` 分支 → `hostGroupContainerEl: null` + `slotKey: null`）。这一个返回值同时关掉了三样反馈：header 高亮（`drag-into` 需 into-group）、蓝色竖条（需 `hostGroupContainerEl`）、hover 展开（`pointerGroupId` 取自 `hostGroupContainerEl`）—— 用户既看不到落点提示，收起的文件夹也"打不开"，来源最后落进 ungrouped 区（渲染在哪就在哪、不在光标处）。根因是 hover 展开与落点 intent 被 `hostGroupContainerEl` 绑死。修复：新增纯函数 `resolvePointerOverCollapsedGroupId`，把"光标物理悬停在哪个收起文件夹上"与"落点 intent"解耦 —— source 拖拽时，无论落点解析成 into-group 还是根层级 reorder，只要光标停在某个收起非空文件夹的 header 上就 arm hover-expand，自动显示 `.sp-hover-expand-pending` 渐显提示、dwell 1000ms 后展开，展开后其 children 渲染出来、走正常 slot 检测即可精准放入。刻意**不**加 `drag-into`（左半区落点其实去 ungrouped，加 `drag-into` 会误导"落进文件夹"）；用 hover-pending 渐显提示作为诚实反馈。保留 X-split 左半区"逃逸到 ungrouped"的设计（单测 `X-split corridor fires on a COLLAPSED folder` 守护）与 1000ms 防误开延迟；group 拖拽行为不变（无 pointer-over 兜底）。新增 2 个单测：source 拖到收起文件夹左半区 → 显示 hover-pending 提示；dwell 1000ms → 展开。
- **搜索结果计数补 live region (Search-Results Count Now a Polite Live Region)**: **影响**: 屏幕阅读器用户键入筛选时能听到「N 个结果」了。 `#sp-search-count` 在键入筛选时实时更新文本（"N 个结果"）却没有 `aria-live`，屏幕阅读器用户听不到匹配数。给该 span 补 `aria-live="polite"` + `aria-atomic="true"`（沿用 toast / popup 详情同款 live region 模式）；`updateSearchResultCount` 已有的 `hidden` 切换 + 文本写入会触发完整播报。新增 1 个 template 单测断言两个属性存在。
- **reduced-motion 残留 stagger 延迟归零 (Zero Residual Stagger Delays Under Reduced Motion)**: **影响**: 开启系统「减少动态效果」后,残留的逐项延迟出现被彻底归零,元素即时显示。 `@media (prefers-reduced-motion: reduce)` 的通用 `*,*::before,*::after` 规则只把 `animation-duration`/`transition-duration` 钳到 1ms，从不归零 `animation-delay`/`transition-delay`，导致未被 `animation: none` 静默的元素（toolbar `nth-child` transition-delay、search、`.sp-native-label-import-item` 的 `index*24ms`）仍按索引延迟出现。在该通用规则补 `animation-delay: 0ms !important; transition-delay: 0ms !important;`，一处修复覆盖全部残留延迟。现有 reduced-motion 单测扩展断言两条归零声明。
- **批量切换按钮补 aria-pressed + 激活态 (Batch Toggle Button Gets aria-pressed and Active State)**: **影响**: 批量模式按钮现在有按下态视觉反馈,屏幕阅读器也能播报开/关。 `#sp-batch-action-btn` 是开关（每次点击翻转 `state.isBatchMode`）却渲染为普通 `.sp-button`、无 `aria-pressed` 也无视觉激活反馈，与同工具栏的 quick-view / isolate 两个 `aria-pressed` toggle 不一致。该按钮在静态壳里跨 render 持久，故在 `render()` 里随 `state.isBatchMode` 同步设 `aria-pressed` + `is-active`（与 `updateSearchResultCount` 同处的持久元素同步点）；`content-style-text.js` 新增 `.sp-button.sp-toolbar-action.is-active` 规则，复用 `--sp-accent` / `--sp-tag-active-bg` accent-tinted 处理与其它 toggle 视觉统一（未引入新视觉语言）。新增 1 个 render 单测（true/false 两态）+ 1 个样式文本单测。
- **history 跨队列写入丢更新 (Cross-Queue Lost Update on State History)**: **影响**: 手动建恢复点与自动保存同时发生时,不再静默丢失一条历史记录。 `SAVE_STATE` 在队列 key `sourcesPlusState_<id>` 上序列化，但其 payload 同时重写 `sourcesPlusHistory_<id>`（`storagePayload[historyKey] = history`）；`APPEND_STATE_HISTORY` / `LOAD_STATE_HISTORY` 却在队列 key `sourcesPlusHistory_<id>` 上序列化。两者位于**两条独立 FIFO**，对同一 `sourcesPlusHistory_<id>` 做 get→mutate→set —— 当手动创建恢复点（APPEND）与 debounced save 交错时，各自读到写前数组、后落地者覆盖前者，静默丢掉一条历史 / 用户显式创建的恢复点。新增 `getStateKeyFromHistoryKey`（history key → state key 反推），把 `appendStateHistory` / `loadStateHistory` 的队列 key 从 history key 改为派生的 state key，使该笔记本所有 `sourcesPlusHistory_<id>` 写者共享同一条队列；存储 key、读写内容、state 的 revision guard 均不变。新增 1 个 background 单测：SAVE_STATE 在飞时同笔记本 APPEND 被排到同一队列之后（不抢跑、读到刚写入的 history）。
- **teardown 未重置 isDeletingSources (Teardown Left isDeletingSources Stuck)**: **影响**: 切换笔记本的极端时序下,批量界面不再可能卡住。 `resetManagerRuntimeState()` 清掉 `pendingBatchKeys`/`clickQueue`/`isBatchMode` 等却漏了 `isDeletingSources`（test-only `_resetState` 反而重置了），若新 manager 实例在旧批量删除循环的 finally 跑完前挂载，会继承卡住的 flag、楔住批量 UI（批量入口门 `!isDeletingSources` 与命令门）。在 `resetManagerRuntimeState()` 的批量状态归零处补 `isDeletingSources = false;`，使生产 teardown 与 test reset 一致。新增 1 个 lifecycle 单测走真实 `destroyContentInstance → teardown → resetManagerRuntimeState` 路径断言归零。
- **配额预警/裁剪只算单次写 payload、忽略其它笔记本 (Quota Warning/Trim Measured Only the Single Write, Ignoring Other Notebooks)**: **影响**: 存储接近写满时的预警与自动裁剪改按所有笔记本的真实占用触发,不再几乎不触发。 `createStorageUsageInfo` 的 `storageUsageBytes`/`storageUsageRatio` 仅由本次 `SAVE_STATE` 的 payload（单个笔记本的 state+backup+history 三键）算出，全代码库从不调用 `getBytesInUse` —— 于是 30 个各 1MB 的笔记本真实占用约 30MB，但每次写仍报极低 ratio，远低于 0.8 预警 / 0.95 临界阈值，使主动预警 toast、临界 history 裁剪、shrink-only 防锁死门几乎永不触发（真实 `MAX_BYTES` 拒绝仍由 set 回调的 `lastError` 反应式 backstop 兜底，故无数据丢失，但失去提前预警）。新增 best-effort `resolveExtraStorageBytes`：用 `chrome.storage.local.getBytesInUse(null)` 取真实总量、减去本笔记本现有 keys 得「其它笔记本占用」，作为 `extraBytes` 投影进 `createStorageUsageInfo`，让 ratio 反映投影后的总占用。`extraBytes` 默认 0、且 `getBytesInUse` 不可用/出错时回退 0 —— 即完全等同旧的 per-payload 行为，无环境回归。临界 shrink-only 逃逸（比较本笔记本 incoming vs current 字节）与反应式 backstop 字节不变。`writeStateWithRevisionGuard` 与 `appendStateHistoryNow` 都接入。新增 2 个 background 单测（真实总量驱动 warning；投影临界时增长写被拒），并以"中和实现 → 变红"验证其有效性；现有 66 个 background 单测走回退路径全绿不回归。
- **面板分区与缩放手柄补齐可访问性语义 (Accessibility Semantics for Panel Regions and the Resize Handle)**: **影响**: 面板缩放手柄支持键盘方向键调整了,各操作区也补齐了屏幕阅读器语义。 三处关联控件/动态区缺少 landmark/分组语义与可访问名:(1) quick-view rail(一组互关联视图过滤器)补 `role="group"` + `aria-label`(`ui_quick_view_rail_label`);(2) 批量操作栏(随批量模式注入/移除的一组命令按钮)补 `role="toolbar"` + `aria-label`(`ui_batch_actions_region`);(3) 面板缩放手柄此前仅鼠标可用,补 `role="separator"` + `aria-orientation="horizontal"` + `aria-label`(`ui_panel_resizer_label`)+ `tabindex=0`,并新增 ArrowUp/ArrowDown 键盘缩放(步进 16px,按 doDrag 同款 per-view 最小高度 48/150 钳制,经 `saveState({immediate,critical})` 持久化,`preventDefault` 防列表滚动),键盘/触控用户也能调整持久化的面板高度。键盘步进逻辑抽为纯函数 `resolveKeyboardResizeHeight` 并单测(步进/min 钳制/非箭头键 null)。3 个新 i18n key 三语(en/es/zh_CN)同步。新增 3 个模板/渲染单测断言 role/aria 语义。
- **批量选择残留幽灵键污染计数 (Stale Batch-Selection Keys Inflated the Delete Count)**: **影响**: 批量删除时若某选中来源被原生界面移除,删除数量不再被多算、不再弹多余错误提示。 `pendingBatchKeys` 仅在单删 / 全清时剪枝;批量会话中某选中源被 NotebookLM 原生 UI / SPA rescan 移除(非显式用户删除)时,该键残留在集合里,使删除计数 badge 与「Deleting N」toast 反映幽灵键(move/ungroup/tag 路径已 `if (sourceData)` 过滤,故无数据风险,仅计数误导 + 一条多余错误 toast)。在 `render()` 入口(仅 batch 模式)按 `sourcesByKey` 对账剪枝 `pendingBatchKeys`——集合很小、仅 batch 模式非空,成本可忽略。新增 1 个 render 单测断言不在 sourcesByKey 的键被剪、存活键保留。
- **新建文件夹的 pop 动画因特异性被覆盖永不播放 (New-Folder Pop Animation Never Played Due to Specificity Override)**: **影响**: 新建文件夹现在会播放更明显的顶部弹入动画。 新建分组容器同时带 `sp-list-item-enter` 与 `sp-folder-enter`（content-render.js:1361）。`.group-container.sp-list-item-enter`（特异性 0,2,0）与 `.sp-folder-enter`（0,1,0）都用 `animation` 简写，高特异性的 list-item 规则胜出，`sp-folder-pop`（translateY/scale 顶锚弹入）永不运行 —— 新文件夹只播放普通行进入动画、少了更明显的顶部弹入。把 `.sp-folder-enter` 提升为 `.group-container.sp-folder-enter`（0,2,0，且源序在后），让 pop 在新建文件夹上正确胜出；reduced-motion 仍用 `.sp-folder-enter`（`animation: none !important`，`!important` 压过特异性）抑制，故 reduced-motion 行为不变。新增 1 个样式文本单测断言提升后的选择器带 `sp-folder-pop`、且无裸 `.sp-folder-enter {`。

### Security
- **后台把存储 key 绑定到发送方笔记本 (Bind Storage Key to the Sender Tab's Own Notebook)**: **影响**: 堵住同源下一个笔记本页理论上读写另一个笔记本数据的越权面。 background 此前只校验消息来自 `notebooklm.google.com/notebook/` 标签页，但从不校验 `request.key` 的 projectId 与发送方标签自己的 notebook 一致 —— 同源下某个笔记本页的脚本理论上可对另一个笔记本的 `sourcesPlusState_/sourcesPlusHistory_` 键读写。新增 `getNotebookProjectIdFromSenderUrl`（取 `/notebook/` 后第一段）+ `senderOwnsNotebookKey`，对 `SAVE_STATE` / `LOAD_STATE` / `APPEND_STATE_HISTORY` / `LOAD_STATE_HISTORY` 要求 `request.key` 以 `_<sender projectId>` 结尾，否则回 `unauthorized_sender`。取不到 projectId（裸 `/notebook/`）时不新增拒绝，避免误伤。生产中 content script 永远发自己笔记本的 key，故无回归（smoke 真实 SAVE_STATE 全过）。新增 1 个 background 单测（123 标签发 999 键被拒），并给少数沿用任意 key 的既有/新单测配上匹配 sender。
- **导入 customHeight 数值校验 (Validate Imported customHeight Numerically)**: **影响**: 导入数据里非法的面板高度值不再被原样写入存储。 `customHeight` 会被直接写入 `style.height`，但导入/持久化的值此前未做数值校验，非数值或负值字符串会原样残留在存储里。在 `normalizeLoadedState` 新增 `normalizeCustomHeight`：强制为有限正数否则归 `null`（resize 句柄 + CSS 仍在交互/渲染时强制 view 相关的 min/max），收口 4 处 schema 分支。新增 1 个单测覆盖非数值/负/0/对象 → null、有效数保留。
- **导入 `__proto__` 源键不再污染 sourceStateById (Guard Against `__proto__` Source Keys Corrupting sourceStateById)**: **影响**: 导入含恶意 __proto__ 源键时,不再污染内部状态或静默丢源。 `buildPersistableState` 以字面量对象 + `sourceStateById[sourceKey] = record` 重建；导入往返后 sourceKey 受攻击者影响，`sourceStateById['__proto__'] = record` 会改写该对象的原型（而非建自有键），使该源在序列化时静默消失、且对象被污染。在 forEach 顶部跳过 `__proto__`/`constructor`/`prototype` 危险键。无全局原型污染逃逸（groupsById/tag 走 Object.fromEntries/Map），属窄边界完整性修复。新增 1 个单测断言带 `__proto__` 源键时 `sourceStateById` 原型仍为 `Object.prototype`、安全源保留。
- **诊断 / 开发者日志导出脱敏原生 label 标题 (Redact Native Label Titles From Diagnostics and Developer-Log Exports)**: **影响**: 复制诊断信息 / 导出开发者日志时,不再泄露原生标签的标题文本。 原生 NotebookLM 标签导入后，未信任的文件夹 / 标签标题原样存入 `lastNativeLabelImportSummary.labels[].title`，并经 `cloneNativeLabelImportSummary` 浅克隆后嵌入诊断对象 —— 该对象同时进入剪贴板诊断（`getDiagnosticsText`）和开发者日志导出（`getDeveloperLogExportText` 的 `diagnostics:` 块），而 logger 的 `sanitizeDetails`（含 `title|label|name` 脱敏）只作用于 per-entry details、不覆盖 diagnostics 块，违反 SECURITY_THREAT_MODEL「诊断刻意省略标题」约定。把 `cloneNativeLabelImportSummary` 改为按白名单只发出 `{ sourceCount, action }`、丢弃 `title`（单一收口点同时堵住两个导出面；聚合计数等非敏感字段保留，内存里 `labels[].title` 不变供非诊断消费者）。现有诊断单测改为断言克隆不含 `title`、保留 `sourceCount`/`action` 且仍为脱离引用的对象图。

### Removed
- **清理已废弃的复选框绘制动画死 CSS + 同步 UI_GUIDELINES §9.1 (Remove Dead Checkbox-Draw Animation CSS + Sync UI_GUIDELINES §9.1)**: **影响**: 对扩展使用无可感知变化;删除永不触发的复选框动画死代码并同步文档。 `.sp-checkbox.is-animating` 的 organic 对勾绘制(`@keyframes check-draw-organic`)+ spring pop(`@keyframes checkbox-spring`)在 CSS 完整实现甚至带 reduced-motion override,但全代码库**没有任何路径添加 `is-animating` 类**——是不会触发的死 CSS,而 UI_GUIDELINES §9.1 仍称其可用。删除两个 keyframe + `.sp-checkbox.is-animating:checked` / `::before` 规则 + reduced-motion 条目(保留活跃的 `.sp-checkbox:checked::before` 静态对勾)。重写 §9.1:对勾静态绘制、无动画 draw-in,故程序化 / 批量同步不闪、不重放 pop;如需重新引入,在用户直接 toggle 时加类、`animationend` 清除。
- **清理已废弃的 drop fly-in / scaleY 着陆死 CSS + 同步 UI_GUIDELINES §13.4 (Remove Dead Drop Fly-in / scaleY Landing CSS + Sync UI_GUIDELINES §13.4)**: **影响**: 对扩展使用无可感知变化;删除已废弃的拖放着陆动画死代码并同步文档。 `applyDropLandingAndFlash` 早已改写为方向中性的 opacity 淡入 + 兄弟 FLIP（`.sp-drop-shift`），`.sp-drop-flying` / `.sp-drop-landing` / `@keyframes sp-drop-landing-anim` 在全代码库**只被 `classList.remove` 防御性清理、从不被 add**，`.sp-drop-landed` 已彻底消失（content-tree 单测已断言这些 class 不被 add）—— 是不会被应用的死 CSS，而 UI_GUIDELINES §13.4 仍把 fly-in/scaleY/landed flash 当作现行契约（编辑者会改无效 keyframe、或信 200ms 而代码实为 `var(--sp-motion-base)`=180ms）。删除 content-style-text.js 的 `.sp-drop-landing` 规则 + `@keyframes sp-drop-landing-anim` + `.sp-drop-flying` 规则，及 reduced-motion 块里对应条目（保留仍在用的 `.sp-drop-shift` / `.sp-drag-unfolding`）。重写 UI_GUIDELINES §13.4：类清单、落地动画行为、Motion（改为 `var(--sp-motion-base) var(--sp-ease-emphasized)` token 描述）、Rules（改为"勿重新引入方向性 fly-in/scaleY/flash"）与 preflight 条目都对齐当前实现。`handleDragStart` preflight 对 `.sp-drop-flying`/`.sp-drop-landing` 的防御性 remove 刻意保留（近免费、防未来重引入）。

## [2026-05-29] [26.5.29]

### Added
- **外观自定义 (Appearance Customization)**: **影响**: 设置里新增「外观自定义」分区,可一键关掉来源行/分组头的悬浮蓝色光晕。 设置 modal 新增「外观自定义」分区，首项为来源行/分组头悬浮蓝色光晕（spotlight）的总开关，关闭后伪元素 `::before` 直接 `display: none`、跳过过渡 + GPU 合成。偏好以嵌套形式持久化为 `PREFERENCES_KEY.appearance.hoverSpotlightEnabled`，默认 `true`；background SW 的 `mergePreferences` 对 `appearance` 做深合并保留未来兄弟字段；content runtime 在 host mount + 偏好加载完成 + setter 后调用 `applyAppearancePreferencesToHost()` 给 `#sources-plus-root` shadow host 加/移 `sp-appearance-no-spotlight` class，CSS `:host(.sp-appearance-no-spotlight) .source-item.sp-spotlight-surface::before { display: none; }` 实现零 re-render 视觉切换。3 个 locale (en/es/zh_CN) 同步 6 个新 i18n key。
- **物理拖拽让位 (Physical Drag Reflow)**: **影响**: 拖动来源时,目标位置之后的同级项整体让位、形成跟随鼠标的空槽。 拖动来源时被拖项 height/opacity 折叠为 0 离开列表，目标位置之后的同级项整体 `translateY(N × itemHeight)` 让位形成跟随鼠标的空槽，drop/dragend 时反向归零。覆盖单源/多源、根列表/组内、跨组所有场景；过渡统一为 `200ms cubic-bezier(0.2, 0, 0, 1)`。新增 `src/content/content-drag-reflow.js` 模块封装 `prepareDragSession`/`foldDraggedItems`/`computeReflow`/`applyReflow`/`clearReflow`/`unfoldDraggedItems` 并在 `content-tree-interactions.js` 的 dragstart/dragover/drop/dragend 接入。
- **Drop 着陆动画 + 落地高亮 (Drop Landing Animation + Flash)**: **影响**: 松手后被拖项带落地动画飞入新位置(该着陆动画在后续版本又有多次调整)。 放手后落地元素同时启动两条动画。单源拖拽走 FLIP "fly-in" 路径（`.sp-drop-flying`）：读取 cursor drop 位置 + 落地元素 rect，给元素先设 inline `transform: translate(dx, dy)` 让它出现在 cursor 处，强制 reflow 后加 transition 把 transform 清零 — 视觉上 source 从 cursor 平滑飞到 slot（280ms cubic-bezier）。多源 / group 拖拽走 `.sp-drop-landing` scaleY(0→1) 展开（200ms cubic-bezier），多个落地元素从同一 cursor 点散开会视觉混乱所以不用 fly-in。两条 path 都并行播放 `.sp-drop-landed` 600ms accent outline flash 帮助视线 catch 新位置。新增 `applyDropLandingAndFlash(landedKeys, cursorX, cursorY)` helper，自动按 keys 数量选 fly-in vs scaleY。`@media (prefers-reduced-motion: reduce)` 下两条动画都禁用，setTimeout 800ms backstop 兜底清理 class。

### Changed
- **更新「更新介绍」页 + 版本号 → 26.5.29 (Refresh What's-New Page + Bump to 26.5.29)**: **影响**: 升级后的「更新介绍」弹窗换成本轮两个亮点(拖拽优化、稳定性修复)。 升级后弹出的「更新介绍」页换成本轮发布的 2 个亮点——「拖拽功能优化」（折叠让位开槽 + 拖入文件夹蓝条指引 + 更顺滑跟手）与「稳定性修复」（偶发抖动/闪烁、存储写满删不掉来源的死锁、多处边界与时序问题）（旧的"批量拖拽 / 悬停展开 0.6 秒 / 智能反馈"是更早版本的，且 0.6 秒已过时）。i18n key 同步改名 `ui_whats_new_{drag,stability}_{title,body}`（en/es/zh_CN 三语言）、modal icon 换 `drag_pan` / `verified`、subtitle 改为"这次的主要更新"。版本号 `manifest.json` + `package.json` + README badge 同步到 `26.5.29`（日期版）；What's New 用完整版本号比较，老用户升级后会再次看到更新页。相关单测（locales 必备 key、`content-modal-whats-new` featureRow 断言、`content-modals-tags` 渲染断言）同步更新。
- **拖拽 hover-expand 延迟 600 → 1000ms (Drag Hover-Expand Delay 600 → 1000ms)**: **影响**: 拖动经过收起文件夹时,自动展开的等待从 0.6 秒延长到 1 秒,减少误打开。 拖动经过 collapsed folder 时自动展开的 dwell 时长从 600ms 提到 1000ms（hover-collapse 同步抬高到 1000ms 保持对称），减少"拖动文件夹路过别的文件夹时被误打开"的概率。用户耐心阈值类参数，仍保持 hardcoded（项目惯例，不引入抽象）。同步改 `armHoverExpandTimerForGroup` / `armHoverCollapseTimerForGroup` 的 setTimeout 与 `.group-container.sp-hover-expand-pending > .group-header` 的 600ms keyframe build-up 到 1000ms（CSS 与 timer 必须同时长，否则 outline 会跑在打开前/后），并把单测 9 处 `jest.advanceTimersByTime(600)` 同步到 1000。提醒：X-split（"右半进入 / 左半避让"）只对 collapsed top-level folder 的 header Y 范围生效 —— 此次只动延迟，未扩展 X-split 范围。
- **拖拽动画 token 统一 (Drag Animation Tokenization)**: **影响**: 拖拽动画统一到 180ms 节拍、起步更跟手(整体快约 10%);视觉差异很小。 把 content-style-text.js 拖拽段 5 个 class (`.sp-drag-folded`/`.sp-drop-shift`/`.sp-drop-landing`/`.sp-drop-flying`/`.sp-drag-unfolding`) 共 14 处硬编码 `200ms cubic-bezier(0.2, 0, 0, 1)` 统一到 `var(--sp-motion-base) var(--sp-ease-emphasized)` (180ms emphasized),按 UI_GUIDELINES line 311-317 拖拽属于 reveal/entry 范畴用 emphasized,line 334 明示 "use motion tokens instead of raw durations"。同步把 PR-2 加的 `.sp-drag-cancelled` animation 时长改 token (`var(--sp-motion-base)` 替 200ms,easing 仍 standard 因 shake 是状态变化非 reveal);`.sp-hover-expand-pending` 600ms 保留 hardcoded(用户耐心阈值非动画节奏)。配套 JS 常量 `content-drag-reflow.js DEFAULT_TRANSITION_MS` 200→180 与 CSS 对齐,公开测试断言同步。视觉影响:拖拽动画快 10% (200→180ms),起步更 snappy (emphasized 曲线),与 collapse/expand/list-entry/toast/modal 同节拍。token 覆盖率从 92.9% 升到约 96%。
- **拖拽反馈三项收尾 (Drag Feedback Polish: Hover Highlight Transition, Cancel Cue, Hover-Expand Pre-Hint)**: **影响**: 目标分组高亮渐变、取消拖拽时红色 shake 提示、悬停展开前的渐显预提示三项视觉补完。 三项共享 motion token 的拖拽视觉补完合一发车。(1) `.group-container.drag-into > .group-header` 给目标分组高亮加 120ms `background-color + border-radius` 过渡,从瞬切色改为渐变,跨入/离开分组时引导感连续。(2) dragend 但未成功落地的取消路径(判据:`runtime.dragReflowSession` 仍非 null,drop 成功路径已在 line 2390 置 null)在 `cleanupReflowSession` 内给每个被拖元素加 `.sp-drag-cancelled` class,CSS 触发 200ms `translateX` shake + 红色 `box-shadow` 与 240ms transition fade-out,让用户明确感知"被拒"而非"自然回弹";240ms setTimeout 移除 class。(3) hover-expand 600ms 等待期不再哑等 —— `armHoverExpandTimerForGroup` 在 timer 启动同时给 host group-container 加 `.sp-hover-expand-pending`,CSS 600ms `forwards` keyframe build-up inset outline 从 0 渐变到 `var(--sp-drag-into-bg)` 同色 2px,timer fire/cancel 时同步移除 class(timer entry 携带 containerEl 引用,避免重复 query)。三项全部遵守 `prefers-reduced-motion`:取消路径保留红框跳过 shake/transition;预提示保留静态 outline 跳过 build-up keyframe;高亮过渡禁用。5 个新单测覆盖 add/remove 时机 + 成功路径不加 cancelled class + 跨 group 转移 pending class。
- **Dragover 性能节流 (Drag-over rAF Coalescing)**: **影响**: 大列表/深嵌套拖拽时每帧只计算一次,卡顿明显减少(行为不变)。 `handleDragOver` 用 `requestAnimationFrame` 合并连续 dragover 事件,将 `computeDropIntent` + reflow shift 计算 + cross-host 祖先链 walk + autoScroll 速度刷新统一收敛到每帧一次。`e.preventDefault()` 留在同步路径(浏览器要求,否则 dropEffect='none' 抑制 drop),仅快照 `{clientX, clientY, dataTransfer}` 转交 rAF 回调,后续 dragover 在同一帧内只覆盖最新 args、不重复 schedule。`clearDragFeedback` 内 `cancelAnimationFrame` 兜底防止 cleanup 后 stale rAF 执行污染 DOM。无 rAF 环境(jsdom 默认)走同步 fallback,现有 1011 单测无回归;新增 4 单测覆盖单帧合并、preventDefault 同步、最新快照采纳、cancel 路径。大列表/深嵌套场景每秒数十次 dragover 不再每次都触发 layout-affecting 工作。
- **拖拽动画一致性收尾 (Drag Animation Consistency Polish)**: **影响**: 拖拽各阶段时长/缓动/无障碍对齐到同一节拍,取消拖拽时平滑回位、不再「啪」地出现。 把整个拖拽系统的时长 / 缓动 / a11y 都对齐到同一节拍。Fly-in 时长从 280ms 改回 200ms 与 reflow / scaleY landing 对齐；落地 flash 的 `ease-out` 改为 `cubic-bezier(0.2, 0, 0, 1)` 与其他阶段同步；`prefers-reduced-motion: reduce` 媒体查询补齐 `.sp-drop-shift` / `.sp-drag-unfolding` 也禁用 transition；ESC 取消 / 拖到非法区域时被拖项从 height=0 平滑 200ms 展开回原位（新增 `.sp-drag-unfolding` class + `unfoldDraggedItems({ animated: true })` 路径），不再"啪"地出现；handleDragStart 入口加 preflight 清理上一次 drop 残留的 `.sp-drop-flying` / `.sp-drop-landing` / `.sp-drop-landed` / `.sp-drag-unfolding` class（防止新 drag 800ms 内启动时上一次的 transition 规则减慢新 fold）。
- **拖拽 ghost 简化 (Drag Ghost Simplification)**: `.sp-drag-ghost` 移除 `drag_indicator` 图标，仅显示拖动数量数字；单源拖拽（count=1）也启用自定义 ghost，与多源视觉统一。
- **拖拽 Ghost = Source 行克隆 (Drag Ghost = Source-Item Clone)**: **影响**: 拖动虚影从「数字标签」换成真实来源行的克隆,多源拖拽堆叠显示并带数量徽章。 ghost 从“数字 pill”改为真实 source-item 行的 1:1 视觉克隆（含 icon / 标题 / 标签），整个 ghost scale 0.95 模拟“抓起”手感。多源拖拽时堆叠前 3 项克隆（错位 + 旋转 + 阴影），右上角圆形 badge 显示总数 N。setDragImage offset 基于鼠标在原行内的实际位置，让 ghost 在指针下自然对齐。
- **无效拖放视觉 (Invalid Drop Visual)**: 非法 drop 的红色提示从被悬停整项的 box-shadow 改为目标空槽顶部项的 `.drag-invalid` outline 描边，避免被行 `:hover` 阴影覆盖；分组 into-group 仍保留 `.group-container.drag-invalid > .group-header` 红色头部高亮。
- **拖拽插入位置改为 slot-based 几何检测 (Slot-Based Drop-Intent Detection)**: **影响**: 拖放落点改用几何检测,根治同级项抽搐、修好嵌套文件夹空隙不打开的问题。 用 pointer-Y → list → slot 的几何映射替代 closest()-based dropTarget 识别，根治 reflow shift 反馈循环导致的 sibling 抽搐，并修复嵌套 group 场景下 group-container 覆盖 source-item 导致空隙不打开的 regression。新增 `computeDropIntent` helper（一次性查询 root，按 parentMap depth 选最深 group-container，所有 rect.top 都减去 `extractInlineTranslateY` 得到未偏移布局位置）；删除旧的 `getDropIntent` / `findStableDropTarget` / `computeDropPositionStable` / `computeDropPosition` / `normalizeIntentKind` 与 deadzone hysteresis state；`extractInlineTranslateY` 移到 `content-drag-reflow.js` 导出，便于跨模块共用。`computeIsInvalidDrop` 的 source-single 分支补齐 top-level before-group / after-group 拒绝，与 source-multi 对称，避免单源 drop 到根级 group 间隙时插入索引落空。
- **抽取共享 preference normalizers (Shared Preference Normalizers Extracted)**: **影响**: 对扩展使用无可感知变化;消除前后台重复代码的纯重构。 把 `src/background/index.js` 与 `src/content/content-developer-logger.js` 之间逐字重复的 10 个 `normalizeXxx` 函数(~130 行)抽到新模块 [src/utils/preference-normalizers.js](src/utils/preference-normalizers.js),挂在 `globalThis.NSM_PREFERENCE_NORMALIZERS`。Content script 通过 manifest content_scripts 加载(在 `src/utils/index.js` 之后、`content-developer-logger.js` 之前);background SW 顶部用 `importScripts('../utils/preference-normalizers.js')` 加载,Node test 环境走 `require` fallback。两个生产文件改用 `const { normalize... } = globalThis.NSM_PREFERENCE_NORMALIZERS` destructure 保持 ~28 处调用点不变;`background/index.js` 同时删去原 `QUICK_VIEW_BUTTON_KINDS` 常量(已迁入 utils)。`tests/helpers/load-content-module.js` + `tests/helpers/content-test-harness.js` + `eslint.config.js` 同步注册新全局;`tests/background.test.js` 与 `tests/content/content-developer-logger.test.js` 顶部 preload 共享模块。零行为变化:lint 0/0、1018 unit tests + 18 smoke tests 全过、`npm run package` 64 文件 zip 正常。未来 normalize 规则变化(新增语言 / shortcut alias / quick view kind)只需改 utils 文件一处。
- **Factory 入口 JSDoc (Factory Entry JSDoc for AI-Agent Onboarding)**: **影响**: 对扩展使用无可感知变化;只给模块加 JSDoc 契约注释。 给 42 个 `src/content/*.js` factory(`createContent*` / `createManagerShell`)和 `src/background/index.js` 顶部加 JSDoc 签名 — 列出 `deps` 的必需 key 与 `@returns` 暴露的方法名,让 agent 探索新模块时前 5-30 行就能读到契约,不必扒 40 行 `typeof deps.X === 'function'` null-coalesce 模式推断 deps shape。分级处理:小模块 (< 150 行) 紧凑 4-8 行 JSDoc;中模块 (150-500 行) 列全 Required deps + 主要 returns;大模块 (> 500 行,例如 [content-render.js](src/content/content-render.js)、[content-persistence.js](src/content/content-persistence.js)、[content-source-sync.js](src/content/content-source-sync.js)、[content-tree-interactions.js](src/content/content-tree-interactions.js))分类列举 deps + "完整 deps / return 块见 line N" 行号指针。`background/index.js` 文件级 JSDoc 标明 `normalizeXxx` 与 `content-developer-logger.js` 存在 verbatim 复制(下一 commit 用 Phase 3 抽取共享)。共 +701 行注释,零代码改动。零行为变化、零 UI 变化;lint 0/0、1018 unit tests 全过。
- **代码内 invariant 护栏 (Inline Invariant Comments for AI-Agent Navigation)**: **影响**: 对扩展使用无可感知变化;把关键不变量就近落到代码注释。 把 CLAUDE.md "Non-obvious gotchas" 的 7 处关键 invariant 就近落到代码 inline 注释,让 agent grep 函数时立即看到护栏不必先读 CLAUDE.md:`state.groups` 是 string[] 非 group 对象 ([src/content/index.js](src/content/index.js))、NotebookLM SPA route 切换 teardown+reinitialize 不整页刷新 ([src/content/index.js](src/content/index.js))、`computeDropIntent` 返回 `targetList` 异构 shape 与 splice 约束 ([src/content/content-tree-interactions.js](src/content/content-tree-interactions.js))、drag ghost 三处 cleanup 协同 (`handleDragEnd` / `clearDragFeedback` / `destroyMultiDragGhost` 跨 [src/content/content-tree-interactions.js](src/content/content-tree-interactions.js) + [src/content/content-drag-multi.js](src/content/content-drag-multi.js) cross-reference)、`source-descriptor-helpers.js` 顶部 SECURITY 提醒 NotebookLM 派生字符串不可信。统一使用 `INVARIANT` / `CROSS-REF` / `BACKSTOP` / `SECURITY` 大写 tag 帮 agent grep 时一眼可 prioritize。零行为变化、零 UI 变化;lint 0/0、相关单测 224/224 全绿。
- **Drop on folder header 改为落到 children 顶部 (Drop on Folder Header Now Lands Source at Top of Children)**: **影响**: 把来源拖到文件夹标题上,展开后它会出现在最顶部(而非末尾),更符合直觉。 之前 drop 在 folder header 上 → source 被追加到 folder children 的**末尾**；改为插入到**顶部**（index 0）。匹配用户心智模型："我把 source 拖到这个 folder 上，folder 展开后应当立刻看到它"。修复 computeDropIntent 5 处 into-group return 把 `insertIndex: -1` (sentinel for "use list.length") 改为 `insertIndex: 0` (actual index)；同步简化 handleDragOver 的 `insertIndexForReflow` 不再特判 into-group（reflow 现在跟实际 drop 位置一致，在 children 顶部开 slot 而不是末尾）；applyMultiSourceDrop 的 batch into-group 从 `push` 改为 `splice(0, 0, ...entries)` 在顶部 bulk insert 保持 batch 选中顺序（不会反转）。2 个 computeDropIntent 单测的 `insertIndex.toBe(-1)` 改为 `toBe(0)`；新增 3 个端到端单测：`single-source drop on folder header lands source at the TOP of existing children` + `multi-source drop on folder header lands batch sources at the TOP preserving their order` + applyMultiSourceDrop 自己的 `moves three sources into a group at the TOP, preserving relative order`。
- **handleDragEnd 去重 hover/feedback cleanup (handleDragEnd Cleanup Dedup — clearDragFeedback as Single Source of Truth)**: **影响**: 对扩展使用无可感知变化;拖拽结束清理逻辑的去重重构。 `clearDragFeedback` 内部已经做 `cancelAllHoverTimers` + collapse hover-expanded groups + `autoScrollController.stop` + `runtime.activeDragContext = null` + 移除 `.dragging`/`.drag-into`/`.drag-invalid` class。原 `handleDragEnd` 在调用 `clearDragFeedback` 之前先把这五件事手动重做一遍（无 bug 但 ownership 不清晰，未来若有人改 `clearDragFeedback` 行为容易引入 regression）。重构让 handleDragEnd 只 delegate 一次 + 专注做 dragend-only 的事：ghost destroy / reflow session teardown / pseudo-hover + synthetic mousemove。无功能变化、现有所有测试继续通过。

### Fixed
- **拖拽 mid-render reflow shift 重应用实为 no-op (Mid-Drag Reflow Re-Apply Was a No-Op)**: **影响**: 拖拽中界面刷新后,同级项不再瞬间跳回原位、闪一下。 `applyReflowAfterRender`（render() 末尾 hook，用于 mid-drag `render()` 重建 DOM 后把记录的 sibling `translateY` shift 重新贴到新节点）实际从不写 DOM —— 它把 `session.shiftedItems` 同一个 Map 同时当 `session` 和 `shifts` 传给 `applyReflow`，diff 循环里 `prev === delta` 对每个条目恒真、跳过所有写入，shift 在 render 后丢失一帧（siblings snap 回 flat）。原有 3 个单测因 mock 掉 `applyReflow` 只断言"参数传对"而没捕获。修复：先 `new Map(live)` 快照再 `live.clear()`，让 `applyReflow` 的短路看到 `prev === undefined` 从而真正重写 `translateY` 并回填 session。新增不 mock `applyReflow`、用真实 reflow 引擎断言新节点拿到 `translateY(40px)` + `.sp-drop-shift` 的 behavior 单测；旧断言契约同步更新为 decoupled snapshot。
- **拖拽离开列表未取消 pending dragover RAF (Dragleave Leaves Queued Dragover Frame Armed)**: **影响**: 光标拖出列表后,不再错误地又挂上一个文件夹自动展开计时器。 光标拖出 `#sources-list` 时 `handleDragLeave` 只调 `cancelAllHoverTimers()`，没取消已 queue 到下一帧的 dragover RAF；该帧仍会 `_processDragOver → armHoverExpandTimerForGroup`，在指针离开列表后又挂上一个 hover-expand timer（唯一取消点 `_cancelPendingDragOver` 此前只在 drop/dragend 的 `clearDragFeedback` 调用）。修复：在 `handleDragLeave` 的 sources-list 分支补 `_cancelPendingDragOver()`。新增单测断言离开列表会 `cancelAnimationFrame` 掉 queued 帧、不再 arm hover timer。
- **hover-collapse 在容器不可达时状态失同步 (Hover-Collapse State Desync When Container Unreachable)**: **影响**: 拖拽中文件夹被外部刷新移除时,不再出现永久展开、收不回的文件夹。 `executeHoverCollapse` 先把 groupId 从 `hoverExpandedGroupIds` 删除、再查 group/DOM；若拖拽期间外部 state sync 重建或移除了 group 容器（`querySelector` 返回 null），旧代码在 `if (!container) return` 处退出 —— Set 已删但 `group.collapsed` 仍 false，该 group 视觉上永久展开且 `disposeHoverOpenedGroupsAfterDrop` 的快照再也收不回它。修复：DOM 不可达时直接 `group.collapsed = true` + `saveState`（state 为真相源，下次 `render()` 收起 DOM），保证无论 DOM 是否可达 group 最终状态都一致。新增单测用 expand → 外部移除 DOM → collapse timer fire 的真实路径锁定行为。
- **拖拽 drop 后 hover 高亮错位 (Post-drop hover stuck on origin row)**: **影响**: 松手后高亮立即落到光标实际所在的行,不再卡在被拖来源的旧位置。 drop 完成后光标下"应该高亮"的那一行没有高亮，反而是被拖来源原来所在的行（现在已经显示别的来源）保持高亮，直到用户真的动一次鼠标。根因有两条叠加：(1) `handleDragEnd` 派发的 synthetic `mousemove` 会冒泡到 document 的 capture listener，立刻把自家刚加上的 `.sp-pseudo-hover` 清掉；(2) CSS 注释里规划的 `.sp-drag-active` 压制层（用于盖住被冻结的 native `:hover`）从未实装。修复：`_onCleanup` 增加 `event.isTrusted === false` 短路，自家 synthetic event 不再触发清理；`handleDragEnd` 在添加 `.sp-pseudo-hover` 的同时给 `#sources-list` 加 `.sp-drag-active`，`_clearPseudo` 同步移除；`content-style-text.js` 补完 `#sources-list.sp-drag-active .source-item:hover` 与配套 spotlight `::before` 的视觉压制规则，并用同样作用域内的 `.sp-pseudo-hover` 规则把高亮重新断言到光标下的行。新增 3 个 unit test 锁住 isTrusted gate + class 注入/移除契约，新增 1 个 smoke 用 `page.mouse.move` 验证真实 trusted 事件能把两个 class 都清掉。
- **dragstart preflight 补全三个残留 cue 清理 (Preflight Now Strips .sp-drag-active / .sp-hover-expand-pending / .sp-drag-cancelled)**: **影响**: 上次拖拽被打断的残留视觉(描边/抖动/hover 抑制)不再污染下一次拖拽。 dragstart 入口的 preflight 此前漏清三个会跨 drag 残留的视觉 class —— `.sp-hover-expand-pending`（group-container 的 1000ms outline build-up）、`.sp-drag-cancelled`（source-item 的取消 shake）、`.sp-drag-active`（挂在 `#sources-list` 自身、压制冻结的 native `:hover`）。当上一次 drag 被 tab 切换 / window blur 中断、它们各自的 timer / setTimeout 没跑完时，残留会污染下一次 drag（stale outline / 重放 shake / hover 抑制错乱）。修复：前两个（后代元素）并入 preflight 第一个 `querySelectorAll` 选择器 + forEach remove；`.sp-drag-active` 因挂在 host 自身而非后代，单独 `_preflightList.classList.remove`。新增单测断言三者都被清。
- **storage quota critical 时连删除 source 都被拒造成死锁 (Quota-Critical Save Rejection Locked Out Source Deletion)**: **影响**: 存储写满时,删除来源这类「释放空间」的操作不再被拒,用户能自救。 background `writeStateWithRevisionGuard` 在 projected `chrome.storage.local` 占用超过 `STORAGE_CRITICAL_RATIO`（0.95）时无条件拒绝 `SAVE_STATE`（`storage_quota_exceeded`），history trim 后仍 critical 也照拒 —— 用户连"删除 source 释放空间"这种**让 state 变小**的写入也被拒，陷入无法自救的死锁。修复：在 critical 拒绝前比较 incoming snapshot 与已存 snapshot 的 `getSerializedByteLength`，只拒绝会**增大**占用的写入；缩小或持平的写入（删除 / 精简）即使 critical 也放行，让用户总能恢复空间。纯增长写入仍受 quota 保护。新增单测：大 state 已存 + 小 incoming + critical → `success`（放行）；现有"增长写入仍 `storage_quota_exceeded`"测试不回归。
- **拖到收起文件夹 header 的边界抖动 (Drag-to-Collapsed-Folder-Header Edge Jitter)**: **影响**: 把来源拖到收起文件夹标题边界微动时,不再在「放入/插到旁边」之间反复跳变抖动。 把来源拖到收起文件夹的 header 上、光标在 header 上/下边界附近微动时，drop intent 在「into-group（文件夹作为放入目标）」和「reorder（文件夹让位插到它前后）」之间反复翻转，文件夹自身及周围元素的 reflow shift 状态随之跳变 = 抖动。根因是边界语义切换（叠加 hover-expand 展开 / mid-drag render 时序），不是检测层反馈循环——检测全用 `unshiftedRect`、reflow 无条件或单向 latch，光标固定时本不振荡。修复：给 `computeDropIntent` 引入 into-group **边界迟滞** —— 上一帧已判定进入某 group 时，该 group 的 container 选择 + header 检测 Y 范围各扩展 `HYSTERESIS_PX`（8px）；进入仍需精确命中 header，离开需明显移出缓冲区才切回 reorder，吸收边界微动。`_processDragOver` 把上一帧 `dragReflowSession.currentIntent` 回喂作 `prevIntent`。新增单测：collapsed header 下方 5px 处，无 `prevIntent` → 不 into-group；`prevIntent` 为同 group into-group → 迟滞保持 into-group。
- **拖拽时文件夹左侧竖条重做：跟随目标 + 预览插入空档 (Folder Drag Guide Bar: Follows Target + Previews Insert Slot)**: **影响**: 拖拽时文件夹左侧的指示竖条会正确跟随光标所在文件夹,并预览插入空档。 重做了拖拽时文件夹左侧竖条的逻辑，解决「蓝条停在源文件夹不跟随」「灰条延伸过头/差一截」两类问题。此前竖条由 `.group-container:hover` 驱动 + 给源文件夹补回被拖项高度（`--sp-fold-comp`），导致：蓝条被 Chrome 冻结的 `:hover` 锁死在**源**文件夹、不跟随；源文件夹竖条又被补回被拖项空间而过长。重做后竖条高度反映该文件夹 reflow 后的**视觉底部**——其他文件夹（含源）灰色、自然到最后一项（源 fold 后自然缩短）；**光标当前所在的文件夹**蓝色、向下延伸一个插入空档（`--sp-slot-comp` = `totalDraggedHeight`），同时覆盖「插到中间（兄弟下移）」和「插到最后（空档）」。`_processDragOver` 每帧把 `.sp-drag-guide` + `--sp-slot-comp` 设在 `intent.hostGroupContainerEl`（光标所在文件夹）并清上一帧；CSS 用绝对 `::before`（`height: calc(100% + var(--sp-slot-comp))`、accent 蓝）画竖条、不进 layout（不干扰 reflow / cross-host）。`handleDragStart` 整个拖拽期间在 `#sources-list` 挂 `.sp-drag-active` 压住源文件夹被冻结的 `:hover` 蓝。每帧 + dragend（`clearDragFeedback`）清、preflight 兜底；回退了上一版的 `foldDraggedItems` 源补偿（`--sp-fold-comp`）。新增单测断言光标所在文件夹 children 拿到 `.sp-drag-guide` + `--sp-slot-comp`。竖条 `::before` 用 `border-left` + `border-bottom-left-radius`（而非 background 填充，后者在 2px 宽上圆角退化、弯不出钩）复现常态竖条左下角的圆角"尾巴"，宽度给到 8px 容纳 6px 圆角弧（仍在 `.group-children` 的 8px padding 内、不压内容）；`height` 挂 `.group-children` 同款过渡（`--sp-motion-slow --sp-ease-emphasized`），蓝条随 hover-expand 展开/收起一起平滑伸缩。
- **drag 进行中 render() 后自动重应用 reflow shift (Re-apply Drag Reflow Shifts After Mid-Drag render())**: **影响**: 拖拽中界面刷新时,同级项不再有一帧跳回原位的闪烁。 修复 drag 进行中如果 `render()` 被触发（NotebookLM SPA `syncSourceToPage` / hover-expand 的 setState）会让 sibling 视觉 snap 回 layout 位置一帧的偶发视觉故障。原因：`patchChildren` / `patchNode` 重建 DOM 时会重写 sibling 的 style 属性 / 替换 element，inline `transform: translateY(N)` 丢；`session.shiftedItems` Map 仍 tracks 正确 delta 但 DOM 看不到，且 dragover 不一定每帧 fire（cursor 静止时 throttle）导致下一帧的 re-apply 来得晚。修复在 `render()` 末尾追加 `deps.onAfterRender` hook（try/catch 兜底），content-tree-interactions 新增 `applyReflowAfterRender()` 函数（read session.shiftedItems → idempotent applyReflow），index.js wiring 把两者通过闭包延迟引用连起来。性能：非 drag 状态下 hook 早退（dragReflowSession === null），cost 接近零。新增 3 个单测：有 active drag + 非空 shifts → 调用 applyReflow 一次；无 drag session → 不调用；drag session 在但 shifts 空 → 不调用。
- **handleDragStart 加 edit 模式守卫 (Edit-Mode Guard in handleDragStart)**: **影响**: 在重命名输入框里拖选文字不再误触发拖拽、打断编辑。 修复 user 在 rename input 或其他 editable control（textarea / contenteditable）内拖选文字时不慎触发 dragstart 中断编辑的问题。原因：`triggerRename` 创建的 input 没有 `draggable="false"`，外层 `.group-header` / `.source-item` 是 `draggable="true"`，所以 input 内 mousedown+move 会冒泡到外层 row 触发 dragstart。修复在 handleDragStart 入口、preflight cleanup 之后、source/group 分发之前加守卫：`e.target.closest('input, textarea, [contenteditable=""], [contenteditable="true"]')` 命中则 preventDefault + return，浏览器不进入拖拽生命周期、cursor 保持文本选择模式。新增单测 `aborts dragstart when e.target is inside an input (rename / text edit guard)` 锁定行为。
- **dragstart preflight 扩展到 sp-drag-folded / sp-drop-shift / 残留 feedback 类 + 重置 runtime drag state (Dragstart Preflight Recovers From Interrupted Prior Drags)**: **影响**: 上次拖拽被中断导致的「突然拖不动/同级项错位」,新拖拽一开始就自动恢复。 加强 dragstart 入口的兜底清理，从浏览器层面无法保证 dragend 触发（window blur / Esc race / page nav / crash 都会跳过 dragend）的各种中断态稳健恢复。原 preflight 只清 4 个 transient 动画 class（`.sp-drop-flying` / `.sp-drop-landing` / `.sp-drag-unfolding` / `.sp-pseudo-hover`），漏掉了两个最危险的残留：**`.sp-drag-folded`** —— 残留时上次拖过的 source 卡在 `height:0 + opacity:0 + pointer-events:none`，用户**完全没法再次拖它**（最经典的"突然拖不动"症状）；**`.sp-drop-shift`** —— 残留时 siblings 卡着 inline `translateY(N)` 视觉错位直到下次 render 重建 DOM。修复扩展 preflight：扫 `.sp-drag-folded` 节点 → 移除 class + 清 `style.height` / `style.opacity`；扫 `.sp-drop-shift` 节点 → 移除 class + 清 `style.transform`；扫残留的 `.drag-into` / `.drag-invalid` / `.dragging` → 移除（dragend 没跑就会留下）。同时 defensively reset 三个 runtime 字段：`runtime.dragReflowSession = null`（新 drag 会用新 session 覆盖，但旧 ref 上的对象可能还有未清理的 shiftedItems Map）、`runtime.activeDragGhost` 走 destroyMultiDragGhost 显式销毁、`runtime.activeDragContext = null`、`autoScrollController.stop()` 防止 scroll loop 漏停。新增单测 `preflight strips lingering .sp-drag-folded / .sp-drop-shift state and resets stale dragReflowSession` 锁定恢复路径。代价：common path 上 querySelectorAll 多 2-3 次，每次都返回 0 个节点；中断态下用户从"拖不动"立即恢复。
- **X-split corridor 只对 collapsed folder 生效 (X-Split Corridor Only Fires on Collapsed Folders)**: **影响**: 已展开文件夹的标题不再套用左右半判定,拖到其标题上正常放入。 进一步收紧 X-split 适用范围 —— 已展开的 folder 完全不应用左右半判定，user 在 expanded folder 的 header 上（无论左半或右半）都会得到正常 into-group 行为。理由：expanded folder 把 children 暴露给 user，user 自然用 Y-axis gap（folder 之间的空隙）来表达 "drop OUTSIDE this folder"；强行在 header 上用 X-split 会跟 "我点 header 想加到 folder 顶部" 的直觉冲突。Collapsed folder 只露出 header 一条带，没有 Y 通道可用，X-split 仍然是有用的 escape gesture。修复 `computeDropIntent` pass 1 在已有的 header Y 范围 + top-level 限制基础上再加 `_grp.collapsed === true` 守卫；expanded folder 直接跳过 X-split 排除集合。新增两条单测 `X-split corridor fires on a COLLAPSED folder` + `X-split corridor does NOT fire on an EXPANDED folder` 锁定行为。
- **X-split corridor 收紧到 header Y 范围 (X-Split Corridor Restricted to Header Y Range)**: **影响**: 在展开文件夹内拖动时,光标偏左不再把整个文件夹误判为「绕开」。 修复用户在已展开 folder 内拖动 source 在另一个 child 上方时、cursor X 落在左半就会把整个 folder 错误排除（dragged source 反而跑去 root level）的 bug。X-split 的"left half = 退出到 root level corridor"逻辑只在概念上对 folder 的 HEADER（视觉上代表"这个 folder 作为一个整体"的展开/收起条）成立 —— 当 cursor 进入 children 区域时用户明显是在操作 folder 内的某个 source，而不是在表达"绕开这个 folder"。修复 `computeDropIntent` 的 pass 1 把 X-split 检测的 Y 范围从 `unshiftedRect(container)`（整个 group container）改为 `unshiftedRect(headerEl)`（只 header strip），cursor 必须落在 header 的 Y 范围内才参与 X-split 排除判定；落在 children 区或 header 之下的任何 gap 都不应用 X-split，slot 检测 / into-group sentinel 正常处理 children-area cursor。新增单测 `X-split applies only to the header Y range — cursor over a child inside the folder uses inside-folder slot detection regardless of X` 锁定行为。
- **Drop 后 hover 强制刷新加 synthetic mousemove dispatch (Force Hover Refresh via Synthetic Mousemove)**: **影响**: 松手后高亮即时刷新到光标所在行,不再要等约 1.5 秒才消失。 修复 drop 完成后 `.sp-pseudo-hover` 1.5s setTimeout 兜底过晚、用户保持鼠标静止时会看到「drop 完高亮 1.5s 后突然消失」的问题。原方案三个 cleanup 监听 `mousemove + mouseover + mousedown` 期望 DOM rebuild 自然 fire mouseover —— 但 `patchNode` 是 in-place 更新（同一 DOM node，只换 class），cursor 下的 element 没换 → 三个监听都不 fire → 撑到 1.5s setTimeout。修复在 `handleDragEnd` 末尾、加完 `.sp-pseudo-hover` 之后主动 `_target.dispatchEvent(new MouseEvent('mousemove', ...))` 强制 Chrome 走 hover-update path，让 native `:hover` 立即接管 cursor 实际所在的 row；synthetic event 通过 capture-phase listener 触发 `_clearPseudo` 同步清掉 pseudo-hover class。pseudo-hover 保留作冗余兜底（覆盖 synthetic-event 在某些 Chromium 路径下被 skip 的情况），两个机制叠加不冲突。
- **Nested group 继承 top-level X-split 排除 (Nested Group Inherits Top-Level X-Split Exclusion)**: **影响**: 想把来源拖出某文件夹到根层级时,不再被它内部的子文件夹误吸进去。 修复 nested group 绕过父级 X-split corridor 的行为不一致 bug。场景：用户拖 source 到 top-level group A 的左半（想"绕开 A 放到 root level"），但 A 含 nested group B 且 cursor Y 也在 B 的 rect 内 —— A 被 X-split 排除（_isTopLevel + clientX < midX），B 没有 X-split 规则（之前的注释说"nested groups keep full-rect rule"），B 作为 candidate 被 chosenContainer 选中 → 用户误入 nested B。修复 `computeDropIntent` 改为两-pass：pass 1 扫一遍 containerList 缓存所有被 X-split 排除的 top-level groupIds；pass 2 主循环对每个 candidate 沿 `parentMap` 向上找 top-level 祖先（self if 已经 top-level），如果祖先在排除集合内 → 整个 candidate 跳过。X-split corridor 现在对祖先链生效，user 在 top-level A 左半 = "我要退出 A 到 root level"无论 A 内有多少 nested group。
- **拖拽 dropped row 加 opacity fade-in (Direction-Neutral Opacity Fade-In for Dropped Rows)**: **影响**: 落地的来源行改为纯淡入、与同级项动画同步,节奏更一致。 修复 drop 时 siblings 平滑 FLIP 但 dropped row "瞬间出现在新位置"导致整体节奏不一致的 UX 问题。之前 `.sp-drop-flying` (cursor→slot FLIP) 和 `.sp-drop-landing` (scaleY 0→1) 都被用户 reject，因为有明显的方向感（"从右边滑入"/"从上往下展开"），所以之前彻底删除了 dropped row 动画。但完全静止又让 dropped row 与 siblings 节奏失同步。修复在 `applyDropLandingAndFlash` 内对 landed rows 应用 `opacity: 0 → ''` 180ms `cubic-bezier(0.2, 0, 0, 1)` fade-in —— 纯 opacity 无 transform 方向感，与 siblings FLIP 同时长同 easing 同步播放。transform 留给 `.sp-pseudo-hover` 的 scale(1.01) 不冲突。`@media (prefers-reduced-motion: reduce)` 下因 transition 一并被禁掉，fade-in 即刻完成。单测 `does NOT animate ...` 重命名为 `fades the landed element in with opacity only`，断言 `opacityWrites === ['0', '']`，transform 写入仍为 `[]`。
- **applyMultiSourceDrop stale list 引用导致 batch drop 后 sources 消失 (applyMultiSourceDrop Stale List Reference Drops Sources)**: **影响**: 批量拖到同一列表内时,来源不再可能从状态里静默丢失。 修复 batch 拖拽到 `before-source` / `after-source` slot 时，若被拖 source 与目标 slot 共用同一 list（root `state.ungrouped` 或同一 group.children），sources 会从 state 中静默丢失的问题。根因：`applyMultiSourceDrop` 进入 splice 分支前把 `intent.targetList` 捕获到本地 `list` 变量，紧接着调用 `helpers.removeSourceFromParent` 触发 `removeSourceFromTree` 的 `state.ungrouped = state.ungrouped.filter(...)` —— **reassign** 而非 in-place mutation，让 `list` 立刻变成 orphan array。后续的 `list.splice(adjustedInsertIndex + i, 0, entry)` 操作在 orphan 上完成，state.ungrouped (现已指向新的过滤后数组) 收不到这些插回的 entry，render() 之后只剩没被拖的 sources。修复在 removes 之后、splices 之前重新从 `intent.targetGroup.children` 或 `state.ungrouped` 读取当前 live 引用作为 `currentList` 再做 splice，state 与视图保持一致。新增单测 `re-resolves the live target list after reassigning removeSourceFromParent (stale-ref regression)` 用 reassignment 风格的 helper 显式覆盖该 codepath（旧单测的 helper 用 in-place splice 与 production 行为不符，故未捕获此 bug）。
- **batch drag smoke 坐标避开 X-split 边界 (Batch Drag Smoke Coordinates Clear X-Split Boundary)**: **影响**: 对扩展使用无可感知变化;修复一个边界坐标导致的冒烟测试偶发失败。 修复 `tests/smoke/batch-drag.smoke.spec.js` 在 `a84e50b`（top-level group container 用 X 左右半判定 inside vs outside）之后稳定失败的问题。根因：测试用 `clientX = rect.left + rect.width / 2`（精确 midX），但 Chrome 在 `new DragEvent(..., { clientX })` 构造合成事件时把 init dict 里的 `clientX/clientY` 整数化（IDL 早期把 `MouseEventInit.clientX` 定义为 `long`，Chromium 实现一直保留）—— 705.635 被 floor 到 705，落在 `_midX (705.635)` 严格左侧 → X-split 判为 outside container → intent flips from `into-group` to `before-source` (on dragged source A itself) → 走到上面提到的 stale-ref bug 路径，drop 后 group 仍为空、A B C 消失，`expect.poll(group children)` 超时。修复测试 clientX 改为 `Math.floor(rect.left + rect.width * 0.75)` 让 cursor 明确在 X-split 右半（inside container），即使 Chrome 整数化也远离边界 ~340px；同步 floor clientY 与之对齐。Production X-split `<` 语义保留不变（midX 自身仍属右半，整数化误差是 Chrome 行为而非 production bug）。
- **拖拽 ghost 文字穿透底色 (Drag Ghost Text Bleed-Through)**: **影响**: 拖动虚影不再与下方真实行文字叠成「双重曝光」。 修复 source-item clone ghost 在跟随光标拖动时与底下真实 source 行文字叠加成「双重曝光」的问题。根因：`inlineStylesRecursive` 把 source-item 的 computed `background: transparent` 写到了 clone（source-item 视觉底色实际依赖父容器 `#sources-list` 的 `var(--sp-panel-bg)`），ghost 离开父容器到 `document.body` 后捕获的 dragImage 位图就是真透明，浏览器拖动时叠在屏幕上自然漏出底层内容。修复给 `.sp-drag-ghost-layer` 显式硬编码 `--sp-panel-bg` 解析值（light `#f6f7f9` / dark `#272c33`）+ `border-radius: 8px` 与 `.source-item` 一致；`@media (prefers-color-scheme: dark)` 补齐 dark mode override。
- **dragstart 瞬间 ghost 覆盖原行 (Dragstart Ghost-Origin Overlap)**: **影响**: 刚开始拖时,虚影不再与原行重叠一帧。 修复刚按下拖动时 ghost 在原 source 行位置渲染、视觉上与原行完美重叠一帧的问题。根因：`handleDragStart` 把 `foldDraggedItems` 包在 `requestAnimationFrame` 里延后一帧执行，而 `setDragImage(ghost)` 是同步完成的；浏览器本帧渲染时被拖项还在原位，ghost 飘在光标上 = 与原行完美叠加。修复改为同步调用 `foldDraggedItems` —— ghost 是独立的 `cloneNode` 在 `document.body`，被拖项 fold 不影响 ghost 内容，原本担心的"ghost 捕获到 fold 状态"在此架构下不成立。对应测试 `'schedules foldDraggedItems via requestAnimationFrame after prepare'` 同步改名 / 改断言为同步路径。
- **拖动中 `:hover` 触发 sibling 抖动 + drop 后 `:hover` 卡在旧行 (Hover Jitter + Post-Drop Hover Stuck)**: **影响**: 拖动经过同级项不再抖动,松手后高亮也不再卡在旧行。 修复两个由 `.source-item:hover` / `.group-header:hover` 引起的视觉问题。第一是拖动中 cursor 经过 reflow shift 的 sibling 时 `:hover` 添加 `transform: scale(1.01) + box-shadow` 与 inline `translateY` 冲突造成视觉跳动；第二是 Chrome 在 drag 期间冻结 `:hover` 状态在 dragstart 时刻光标所指的元素上，drop 后 layout 已变但 hit-test cache 不刷新，导致旧位置那一行仍然是「被悬浮选中」的视觉。修复在 `#sources-list` 上加 `.sp-drag-active` class（`handleDragStart` 加，`handleDragEnd` 清），CSS 规则在 `.sp-drag-active` 期间禁用 `.source-item:hover` / `.group-header:hover` 的 transform / background / shadow / z-index 提升；`handleDragEnd` 末尾给 `#sources-list` 一帧 `pointer-events: none` 再 RAF restore，强制 Chrome 失效 hit-test cache、`:hover` 立即重新计算到 cursor 真实所在的元素。
- **拖拽启动失效回滚 (Revert Sync Fold That Broke Drag Initiation)**: **影响**: 修复上一改动导致的「直接拖不动」,拖拽恢复可用。 撤销前一条 fix 中把 `foldDraggedItems` 从 RAF 改为同步的改动。同步 fold 在 dragstart handler 同步代码内立即给被拖项打 `.sp-drag-folded` class（含 `pointer-events: none` + `padding/margin/border 0 !important`）+ inline `height: 0` + `opacity: 0`，导致 drag source 在 Chrome 完成 drag initiation 之前变成 0×0 box，Chrome 静默 cancel 整个 drag operation —— 用户体感「直接不能拖」。RAF 版让 fold 延后到 Chrome capture ghost + seed drag source rect 之后那一帧执行，drag 已活、只是把视觉藏起来。代价：dragstart 一帧 ghost 与原行视觉重叠的小瑕疵回归 —— 接受，明显比拖不动好。同 commit `04cc341` 中的另外两项保留：`.sp-drag-active` 期间 `:hover` 视觉抑制 + dragend 末尾 `pointer-events: none` 一帧 hit-test cache 刷新，这两项与 drag 启动无关。
- **Fold 让位平滑动画 (Animated Fold Transition)**: **影响**: 开始拖动时,被拖项原位置平滑收起、与下方同级项同步上移,不再瞬间消失。 `.sp-drag-folded` 加 200ms `cubic-bezier(0.2, 0, 0, 1)` transition（height / opacity / padding / margin / border-width），与 `.sp-drag-unfolding` / `.sp-drop-shift` 同时长 + 同 easing。dragstart 时被拖项原位置不再瞬间消失，而是与下方 sibling 平滑同步上移；新插入位置的 reflow shift 同时长打开空槽 —— 三阶段动画节奏统一。早期 instant fold 担忧的「fold height transition 与 reflow translateY transition 不能完美 cancel 产生 jitter」在当前 slot-based 几何检测下（不依赖 layout reflow）已经不成立。
- **Slot 检测改用 visual midY，对称上下避让 (Visual Mid-Y Slot Detection for Symmetric Avoidance)**: **影响**: 向上、向下避让让位现在对称,向上不再需要移动很多才让出空槽。 `computeDropIntent` 比较 cursor 与 candidate slot midY 时不再减 inline `translateY` shift。之前 un-shifted midY 让「向上避让」需要鼠标向上跨过 layout midY（= visual midY 减 slotHeight）才 trigger sibling unshift，与「向下避让」（visual midY 与 layout midY 之差让向下穿越更容易）严重不对称 —— 用户体感「向上必须移动很多才让出空槽」。改用 visual midY（`rect.top + rect.height/2` raw）后两个方向 symmetric，cursor 进入 sibling 视觉上半就立即触发 unshift。stability proof: sibling 只在 shifted 状态下 visual ≠ layout，cursor 跨过 visual midY 后 intent 翻转，sibling unshift，visual mid 回到 layout mid，cursor 仍在新 mid 之外 —— 单向 latch 不振荡。`unshiftedRect` 仍用于 group container / header / children-area 命中（这些不参与 sibling reflow shift）。
- **Drop 后强制 :hover 重新命中 (Force Hover Re-Hit-Test After Drop)**: **影响**: 松手后高亮强制重新命中光标所在来源,不再卡在旧位置。 `handleDragEnd` 末尾在 `pointer-events: none` 一帧 flicker 的 RAF restore 内追加 `dispatchEvent('mousemove')` 到 `elementFromPoint(e.clientX, e.clientY)`（先 shadowRoot，fallback document）。之前单纯 `pointer-events` 翻转不足以让 Chrome 失效 hover-state cache —— synthetic mousemove 显式走 Chrome hover-update 路径，drop 完成后 cursor 实际所在的 source 立即获得 `:hover` 高亮，不再卡在原拖项 DOM 位置。两个机制冗余叠加。
- **Drop 后 hover 改为 JS-managed pseudo-hover (JS-Managed Pseudo-Hover Replaces Native :hover Refresh)**: **影响**: 松手后正确高亮光标所在行,并压住原拖项位置的错位高亮。 上一条 fix 中的 `pointer-events` 翻转 + synthetic mousemove dispatch 在实际使用中**仍然不能**让 Chrome 失效冻结在 dragstart 时刻的 native `:hover` state（synthetic event 的 hover-update 路径被 Chromium 隐式 skip）。重写为不依赖 Chrome `:hover`：(1) 不在 dragend 立即移除 `#sources-list.sp-drag-active`，保持 CSS 规则**抑制**那个错位的 native `:hover` 视觉；(2) JS 找 cursor 实际所在的 `.source-item` / `.group-header`，手动加 `.sp-pseudo-hover` class（CSS 复制 `:hover` 的 background / transform / shadow / z-index 视觉）；(3) 注册一次性 `document.addEventListener('mousemove', cleanup, true)`，第一次真实 mousemove 同时清掉 `sp-drag-active` 和所有 `sp-pseudo-hover`，让真实 `:hover` 接管。drop 完成后 cursor 立即正确高亮、且原拖项位置的错位高亮被压制。`handleDragStart` preflight 列表加 `.sp-pseudo-hover` 防止上次残留干扰下次 drag。
- **抑制原拖项位置 spotlight 蓝色光晕 (Suppress Stuck Spotlight Halo on Original Drag Source)**: **影响**: 松手后蓝色光晕跟随光标实际所在行,不再卡在原拖项位置。 上一条 pseudo-hover fix 处理了 `:hover` 主样式（bg / transform / shadow / z-index），但漏掉了 `.sp-spotlight-surface::before` 的 radial-gradient 蓝色光晕 —— 它由 `:hover::before { opacity: 1 }` 激活，drop 后仍然挂在原拖项位置。补两条规则：`#sources-list.sp-drag-active` 期间把 `.sp-spotlight-surface:hover::before` 的 opacity 强制压回 0；同时 `.sp-spotlight-surface.sp-pseudo-hover::before` 也激活 opacity 1，让光晕跟随 cursor 实际所在的元素。两条规则与现有的 transition 配合产生统一的视觉效果。
- **Reflow shift 跨 host 边界传播 (Cross-Host Reflow Shift Propagation)**: **影响**: 从展开文件夹内往上拖时,外部分组不再挤进来造成视觉重叠。 修复展开的 group 内最末 source 向上拖时，下面外部 sibling（其他分组）挤进 group 内 children 显示位置造成视觉重叠的问题。根因：reflow shift 之前只应用到 `host` 内（即 group 自己的 children），但 fold 让被拖项 collapse 之后 group container 的 layout 高度缩短，外部 root-level sibling layout 上移 —— 而 host 内 children 又被 `translateY(+slotHeight)` shift 推下，超出 group container 视觉边界、与下移过来的外部 sibling 重叠。修复在 `handleDragOver` 计算完 host-internal shifts 之后，沿 host group container 的 DOM 祖先链向上遍历（host → 自己的 `.group-children`'s parent group-container → 直到 `#sources-list`），把每一层「host path 之后的 DOM siblings」也加进 shifts map，全部 shift `+slotHeight`。整个 list 视觉上保持原位（除了 cursor target slot 处打开的空槽）—— 不再有跨边界重叠。嵌套 group 场景也覆盖。
- **Drop 时 sibling FLIP 动画消除「卡一下」感 (Sibling FLIP Animation Eliminates Drop Snap-Back)**: **影响**: 松手时所有同级项与被拖项同步平滑滑到新位置,消除「卡一下」。 修复用户松手 drop 时整体「卡一下」、不丝滑的细节问题。根因：drop 流程中 `clearReflowBeforeMutation()` 清掉所有 sibling 的 inline transform，紧接着 `render()` 重建整个 list DOM —— 旧的 transform 状态随 DOM 销毁而丢失，新 DOM 上没有 transform，sibling **瞬间从 shifted visual 位置 snap 回 layout 位置**，与 dragged 自己的 200ms fly-in 入场不同步，用户感觉到一帧 jank。修复在 `handleDrop` 入口（mutation 前）snapshot 所有 visible `.source-item` / `.group-container` 的 `rect.top` (反映 drop-time visual 位置 = layout + reflow shift)；传到 `applyDropLandingAndFlash`；后者在 fly-in / scaleY landing 之外，额外对每个非 landed 的 sibling 做 FLIP —— 计算 `delta = oldTop - newTop`、设 inline `transform: translateY(delta)` 让它先跳回 drop-time visual 位置、强制 reflow、加 `.sp-drop-shift` class 让 200ms transition 把 transform 清回 0 = 新 layout 位置。所有 sibling 与 dragged 同步 200ms 平滑过渡，drop 完全丝滑。
- **删除 drop 落地 accent flash (Remove Drop Landing Accent Flash)**: **影响**: 松手落地不再有蓝色光圈「闪一下」,视觉更安静。 移除 `.sp-drop-landed` 类、`sp-drop-landed-flash` keyframes 和 `applyDropLandingAndFlash` 中给落地元素加 `.sp-drop-landed` 的代码。这个 600ms `box-shadow: 0 0 0 2px var(--sp-accent) → transparent` 蓝色光圈淡出动画原本是「帮用户的眼睛 catch 新位置」的设计意图，但用户反馈在 drop 那一瞬间感觉像「闪一下」 —— 与已经存在的 fly-in（dragged 从 cursor 平滑滑入 slot）+ sibling FLIP（其他 sibling 同步从 drop-time 位置滑到新 layout 位置）一起播放时，蓝色光圈瞬间出现是多余的视觉噪音。删掉后 drop 的视觉反馈完全由元素的平滑运动提供，更安静、更丝滑。`animationend` listener 也连同 cleanup 一起删除（仅保留 `setTimeout(800ms)` backstop）；preflight 清理列表 + `prefers-reduced-motion` 媒体查询同步去掉相关引用；3 处单测断言更新为「应该 NOT add `sp-drop-landed`」。
- **drop 后抑制 `.sp-list-item-enter` 全列表入场动画 (Suppress `.sp-list-item-enter` After Drop)**: **影响**: 松手后整列来源不再集体微缩放/淡入「抖一下」。 修复用户反馈的「drop 后所有来源都稍微往上微微移了一点点」感觉。根因：`.sp-list-item-enter` 是一个 staggered 入场动画（`animation-delay: calc(var(--sp-list-item-index, 0) * 18ms)`，每行延迟递增），效果是 `opacity 0→1 + scale(0.985→1)`，原本设计用于 list 首次挂载。但 drop 流程中 `render()` 重建整个 list，`patchNode` 给每行的 `class` attribute 都重新写入 `.sp-list-item-enter` —— 触发整个 list **再次播放**入场动画，用户看到所有 source 微微缩放+淡入，被感知为「闪」/「移」。修复在 `applyDropLandingAndFlash` 内部、sibling FLIP 之前，同步遍历 root list 把所有 `.sp-list-item-enter` class 移除。因为浏览器在同步 code 完成前不 paint，移除发生在 animation 实际开始播放之前 —— 入场动画从来不会启动。drop 是 reorder 操作（没有真正"新"行），跳过入场动画是正确的；list 首次挂载不受影响。
- **drop 时抑制 `.source-item` base transform transition (Suppress Base Transform Transition on Drop)**: **影响**: 松手后位置未变的同级项完全静止,彻底消除「往上微移一点点」。 在前面两条 fix 之后用户**仍然**看到「微微往上移」。最终根因定位到 `.source-item` 和 `.group-header` 的**基础 CSS transition rule** 里包含 `transform var(--sp-motion-base) var(--sp-ease-standard)` (180ms)。drop 流程中 `clearReflowBeforeMutation` 把 sibling 的 inline `transform: translateY(N)` 清成 `''` —— 这是 transform 属性的计算值改变，**base CSS transition 自动接管**，给每个 shifted sibling 加上一个 180ms 的"自动滑回"动画。对于 sibling FLIP 跳过的 delta=0 元素（drop 后 layout 位置恰好等于 drag-time visual），用户看到它们从 `translateY(N)` 滑回 0 —— 即 visual 上「往上微移 N px」。修复在 `applyDropLandingAndFlash` 入口（sibling FLIP / fly-in 之前）同步遍历所有可见 `.source-item` / `.group-container` 加 inline `transition: none`，**强制**禁掉 base transition；接着 sibling FLIP 和 dragged fly-in / scaleY landing 在自己改 transform 前**显式**把 inline `transition` 恢复为 `''`，让对应 class（`.sp-drop-shift` / `.sp-drop-flying` / `.sp-drop-landing`）的 transition / animation 规则接管。同步 code 完成后用 double-RAF 把所有"未被 FLIP/fly-in 接管"的剩余行的 inline `transition` 也恢复为 `''`，未来 hover / focus 等正常 CSS transition 不受影响。视觉结果：drop 时只有 dragged 的 fly-in 和 FLIP'd siblings 在动；delta=0 的 sibling **完全静止**（visual 位置与 drop-time 一致，无 auto-slide）。
- **拖文件夹时其他文件夹避让 + 不再无脑嵌套 (Group Drag Reflow + Sibling-Level Reorder)**: **影响**: 拖动文件夹移动位置时,其他文件夹会正确避让,不再被直接拖进目标文件夹。 修复用户反馈的「拖文件夹移动位置时其他文件夹不避让、直接进入到那个文件夹里」。两层 bug：(1) `handleDragStart` 的 group 分支**没调** `prepareDragSession`，所以 `runtime.dragReflowSession` 一直是 null —— 整个 drag 流程没有 fold + 没有 sibling reflow shift = 其他文件夹自然不避让。补上 group 分支也走 `prepareDragSession({ draggedKeys: [groupId], rootElement })` + RAF-deferred `foldDraggedItems`，与 source drag 完全对称。(2) `computeDropIntent` 没区分 source-drag 和 group-drag：cursor 经过 sibling group 的 children-area 一律识别为 `into-group`（嵌套），所以拖文件夹时直接被吸进目标文件夹。修复在 computeDropIntent 内、`activeDragContext.kind === 'group'` 时，把 chosenContainer 的 host 改为「chosenContainer 的 parent group's children」（通过 `parentMap` 查父级；如果 chosenContainer 是 top-level，host 保持 null 走 root-level slot detection），这样 cursor 在另一个 folder 的 body 内 = 「在该 folder 的 sibling level 找 slot」而不是「钻进去」。Header 上 drop 仍然 nest（保留显式嵌套能力）。

### Security
- **拖拽 selector 收敛到 cssEscape 防御 untrusted source key (Harden Drag Selectors With cssEscape for Untrusted Source Keys)**: **影响**: 对扩展使用无可感知变化;加固拖拽元素定位对不可信来源标识的转义。 拖拽定位元素的 4 处 `[data-source-key="..."]` / `[data-group-id="..."]` selector 此前用手写 `String(key).replace(/"/g, ...)` 只转义双引号。source key 是 NotebookLM 派生的不可信输入，带 `]`、反斜杠、前导数字时手写转义不足、`querySelector` 会静默返回 null（safe degrade、非漏洞，但定位失败会导致 reflow / cancel-shake / drop-landing 视觉错乱，且 untrusted 输入迟早会咬）。统一改走 `cssEscape`（内部 `CSS.escape`，无 `CSS` 时 fallback 转义引号 + 反斜杠）：`content-tree-interactions.js` 3 处复用既有 `cssEscape`（2 处 source key + 1 处 group-id），`content-drag-reflow.js` 的 `findItemElement` 因是独立 IIFE 模块、内联一份同实现的本地 `cssEscape`（带交叉引用注释）。新增单测用 `CSS.escape` 哨兵断言 selector 由其输出构建。

### Removed
- **蓝条插入指示 (Blue Bar Insertion Indicator)**: 删除 `.drag-over-top` / `.drag-over-bottom` 蓝条 CSS 规则与对应 JS add/remove 逻辑；让位空槽本身即为插入位置指示。
- **`.dragging` 视觉残留 (.dragging Visual Leftovers)**: **影响**: 对扩展使用无可感知变化;删除被拖项旧的「半透明 lift」残留样式。 旧的 `.source-item.dragging` / `.group-header.dragging` 规则中残留的 `opacity: 0.95` / `transform: scale(1.03) translateY(-2px)` / `box-shadow` / `border` / `background-color` / `z-index` 全部移除。当前架构下被拖项靠 `.sp-drag-folded`（inline `height: 0` + `opacity: 0`）完全消失，旧的 lift 效果只是早期"原地半透明"模式遗留，如果 fold 因任何原因没生效（例如 dragReflow factory 未加载）会显式留下"半透明 lift"残影，与"完全消失"的设计契约冲突。现在 `.dragging` 只保留 `cursor: grabbing` 和 `transition: none`。

## [2026-05-26] [26.5.26]

### Added
- **X 本地发布工具 (Local X Publisher Tool)**: 新增 `marketing/x-publisher/`，提供 OAuth 2.0 PKCE 授权、账号验证和人工确认后发帖的本地脚本，并用本地 `.gitignore` 排除 `.env` 与 `tokens.json`。
- **宣传账号邮箱记录 (Promotion Account Email Record)**: 在宣传账号矩阵中记录插件推广账号统一使用 `notebooklmsourcemanagement@gmail.com` 注册，便于后续社媒账号创建保持一致。
- **宣传资料工作区 (Promotion Workspace)**: 新增 `marketing/` 目录，用于整理社媒账号矩阵、内容日历、平台宣传打法和素材记录，不进入扩展运行时或发布包。
- **快速筛选视图 (Quick Filter Views)**: 在来源管理器工具栏下方新增 All、Ungrouped、Disabled、Tag、Recent 和 Issues 快速视图，支持与搜索词叠加筛选，并为新识别来源保存 `addedAt` 以支持最近新增视图。
- **快速视图按钮自定义 (Quick View Button Customization)**: 设置页新增快速视图按钮管理入口，用户可选择来源面板中显示哪些 quick view 按钮，也可全部隐藏；隐藏后对应命令和自定义快捷键仍可使用。
- **导入差异预览 (Import Diff Preview)**: 导入配置预览新增来源启用状态、文件夹、标签和面板设置差异摘要，并明确提示当前导入仍是恢复/替换当前管理配置而非增量合并。
- **命令面板入口 (Command Palette Entry)**: 在设置页偏好区域新增命令面板入口，支持搜索来源、切换快速视图、切换来源视图、打开设置/标签管理，并在批量模式下执行移动和标签批量命令。
- **命令快捷键自定义 (Command Shortcut Customization)**: 命令面板每个命令新增快捷键设置入口，默认不绑定任何快捷键，用户可自行录入、替换或清除快捷键并保存到全局偏好。
- **更新后介绍弹窗 (What's New Modal)**: 新增按功能版本显示一次的更新介绍弹窗，并在开发者功能区提供测试入口，便于大功能发布时向用户说明变化。
- **本地快照策略 (Local Snapshot Policy)**: 设置页新增历史快照保留数量选择和手动命名恢复点，支持按每个 notebook 保留更多本地版本历史。
- **手动语言切换 (Manual Language Selection)**: 设置页新增 Auto、English、Español、简体中文语言选择，允许扩展 UI 独立于 Chrome UI 语言切换。
- **manifest 与 loader 同步测试 (Manifest <-> Loader Sync Test)**: **影响**: 对扩展使用无可感知变化;新增 manifest 与 loader 加载顺序的同步测试。 新增 `tests/manifest-loader-sync.test.js`，断言 `manifest.json` content_scripts 中 `src/content/*.js` 列表与 `tests/helpers/load-content-module.js` 的 `require` 顺序与文件集合一致，并校验 `clearContentGlobals()` 至少覆盖每个 helper 模块，防止后续抽取模块时测试环境与运行时漂移。
- **状态应用单元测试 (Content State Apply Unit Tests)**: **影响**: 对扩展使用无可感知变化;新增状态应用逻辑的单元测试。 新增 `tests/content/content-state-apply.test.js`，覆盖 `applyPersistableSnapshotToRuntime` 的 dep 校验、`hasPersistableManagerState` 失败短路、`groupsById`/`tagsById`/`sourceTagsById` 清空重写、来源字段 fallback 保留、未在快照树中的来源回填 ungrouped、`activeTagId` 失效清理、`customHeight` 写入与 `syncSourceToPage`/`buildParentMap` 调用计数。
- **撤销历史单元测试 (Content Undo History Unit Tests)**: **影响**: 对扩展使用无可感知变化;新增撤销历史的单元测试。 新增 `tests/content/content-undo-history.test.js`，覆盖 dep 校验、`setUndoBaselineSnapshot` 的当前状态捕获与显式 baseline 克隆、循环引用 signature 防御、签名变化时入栈、`recordUndo:false`/相同签名/撤销期间的不入栈、`stackLimit` 截断、`resetUndoHistoryBaseline` 清空、`undoLastOperation` 空栈/应用拒绝/成功三条主路径以及 `isApplyingUndo` 状态切换。
- **导入导出单元测试 (Content Import/Export Unit Tests)**: **影响**: 对扩展使用无可感知变化;新增导入导出的单元测试。 新增 `tests/content/content-import-export.test.js`，覆盖 factory 校验、`getExportConfigText` envelope 与版本号、`parseImportConfigText` 各类失败原因（empty/invalid JSON/oversize/no-persistable-state/groups 超限/sources 超限/cycle/超深 tree）与 envelope unwrap、`previewImportConfig` 的 matched/unmatched 分区和 enable diff 计数、`applyImportConfig` 主流程（success 路径调用 history/backup/restore/save 与 success toast、deferred 短路、saveState 失败传播）。
- **标签筛选 modal 单元测试 (Tag Filter Modal Unit Tests)**: **影响**: 对扩展使用无可感知变化;新增标签筛选弹窗的单元测试。 新增 `tests/content/content-modal-tag-filter.test.js`，覆盖 dep 校验、`closeTagFilterModal` 传参、`renderTagFilterModal` 在 shadowRoot 缺失时短路、空标签下的 empty-state 节点、按 `state.tagOrder` 排序 + 剩余标签兜底、点击标签触发 `applyTagQuickFilter` + 成功时关闭 / 失败时保留、取消和 backdrop 双关闭路径、`requestAnimationFrame` 异步路径与同步回退、键盘绑定 closeModal 连线。
- **移动到分组 modal 单元测试 (Move-to-Folder Modal Unit Tests)**: **影响**: 对扩展使用无可感知变化;新增「移动到分组」弹窗的单元测试。 新增 `tests/content/content-modal-move.test.js`，覆盖 dep 校验、`closeMoveToFolderModal` 传参、`collectMoveFolderOptions` 的 DFS 平铺 + level 标记 + cycle 防御 + 缺失 group 跳过、`executeMoveToFolder` 的 missing-group 短路 / 单源/多源/字符串源迁移 / batch 模式退出 / saveState 与 render 编排、`renderMoveToFolderModal` 的 shadowRoot 缺失 / 空 keys 短路 / 空分组 empty-state / folder 按钮点击触发迁移 / 缺标题 fallback / backdrop 仅自身点击关闭 / requestAnimationFrame 异步路径 / stagger style 索引递增。
- **欢迎与更新介绍 modal 单元测试 (Welcome & What's New Modal Unit Tests)**: **影响**: 对扩展使用无可感知变化;新增欢迎/更新介绍弹窗的单元测试。 新增 `tests/content/content-modal-welcome.test.js` 与 `tests/content/content-modal-whats-new.test.js`，分别覆盖：欢迎 modal 的 dep 校验、`createWelcomeFeatureRow` icon/title/body 节点构造、shadowRoot 缺失短路、三段 feature row 渲染、关闭按钮 / 主按钮 / 反馈链接 / backdrop 多路径关闭时 `markWelcomeOnboardingSeen` 只触发一次、`openWebStoreFeedback` 仅在反馈链接路径触发、requestAnimationFrame 异步与同步回退、键盘绑定 closeAfterSeen 连线；以及更新介绍 modal 的 dep 校验、`closeWhatsNewModal` 传参、shadowRoot 短路、回退 featureRow 渲染、注入 `createWelcomeFeatureRow` 时改用注入函数、关闭/主按钮 + backdrop 多路径触发 `markWhatsNewSeen` 一次、`options.markSeenOnClose=false` 时不标记、requestAnimationFrame 路径。
- **命令面板 modal 单元测试 (Command Palette Modal Unit Tests)**: **影响**: 对扩展使用无可感知变化;新增命令面板弹窗的单元测试。 新增 `tests/content/content-modal-command-palette.test.js`，覆盖 dep 校验、`closeCommandPaletteModal` 传参、shadowRoot 或 document 缺失短路、空命令列表 empty-state、命令项渲染与首个 active 标记、点击启用/禁用命令分支、executeCommand 返回 false 时不关闭、ArrowUp/Down wrap-around、Enter 执行 active、input 输入触发 resolveCommands + active 重置、mousemove 切换 active、shortcut 按钮启动捕获模式、捕获模式下 keypress 写入 combo、Backspace 清空、Escape 取消、`setCommandShortcut` reject 时弹失败 toast、backdrop 点击关闭。
- **标签 modal 单元测试 (Tag Modal Unit Tests)**: **影响**: 对扩展使用无可感知变化;新增标签弹窗的单元测试。 新增 `tests/content/content-modal-tag.test.js`，覆盖 dep 校验、`closeTagModal`/`closeBatchTagModal` 传参、`getEditTagInputId` 的命名前缀与非法字符替换、`getCssEscapedId` 在 `window.CSS.escape` 可用/缺失下的回退、`executeBatchTagUpdate` 的空源/空标签短路、过滤掉 disabled/loading 源、union/remove 模式语义、批量模式退出 + saveState/render/toast 编排，以及 `renderTagModal`/`renderBatchTagModal` 的 shadowRoot 缺失或空列表短路与正常 mount smoke。
- **设置 modal 单元测试 (Settings Modal Unit Tests)**: **影响**: 对扩展使用无可感知变化;新增设置弹窗的单元测试。 新增 `tests/content/content-modal-settings.test.js`，覆盖 dep 校验、`closeSettingsModal` 传参、`normalizeVisibleQuickViewKinds` 的非数组兜底/canonical 顺序/lowercase trim/未知 kind 丢弃/空数组直通、`renderQuickViewButtonsModal` 与 `renderSettingsModal` 的 shadowRoot 短路与 mount smoke、developer mode 已开启时直接展示开发者区、以及 developer 解锁的安全门：错误密码弹失败 toast + 不调用 removeModalNode、正确密码不弹失败 toast + 调用 removeModalNode 展示开发者区、用户取消 `prompt` 时既不 toast 也不修改 DOM。
- **README 自动化检查章节补全 (README Automated Checks Section Update)**: **影响**: 对扩展使用无可感知变化;补全 README 的自动化检查说明。 在 `README.md` 的 Automated Checks 列表中补充 `npm run lint` 条目（说明 ESLint flat config 的覆盖面与 globals 配置），并把 `npm run verify:full` 的描述从“两套测试”更新为“lint + 单测 + smoke 三步”，与 `package.json` 实际编排和 CI 工作流一致。
- **批量拖拽与多源 ghost (Batch Source Drag with Multi Ghost)**: 批量模式下选中多个来源后，拖任意已选行会一起搬运到目标位置；自定义胶囊状 ghost 显示数量徽章，drop 后弹出 “已移动 N 项来源” toast 并自动退出批量模式。
- **拖拽边缘自动滚动 (Drag Edge Auto-Scroll)**: 拖拽过程中指针靠近 `#sources-list` 顶部或底部 60px 范围时，列表按 RAF 节奏平滑自动滚动，靠边越近滚动越快，越过滚动边界时静默停止。
- **项目 CLAUDE.md 向导 (Project CLAUDE.md Guide)**: **影响**: 对扩展使用无可感知变化;新增项目 CLAUDE.md 开发向导。 在仓库根目录新增 `CLAUDE.md`，指向 AGENTS.md 等权威规则并补充 AGENTS.md 未成文的项目惯例 — factory + globalThis 注册模式、加新 content 模块必须同步的四个文件、Shadow DOM 与 global overlay token 边界、locale `ui_*_toast` 命名、`developerLog` 4 参数签名、`#sources-list` 滚动容器、Playwright 用 synthetic DragEvent 而非 `dragTo` 等。同时在 `docs/PROJECT_DIRECTORY.md` 根目录树中登记 `CLAUDE.md` 条目。
- **拖拽悬停展开折叠组 (Hover-Expand Collapsed Group During Drag)**: 拖拽过程中将光标停留在折叠组头 600ms 后，组会自动展开，允许在一次拖动中送达任意深度的嵌套位置。
- **非法 drop 视觉反馈 (Invalid-Drop Visual Feedback)**: 当 dragover 命中会被现有逻辑静默拒绝的目标（拖源到自身、拖分组到自身后代、拖多源到被拖集合中的成员、批量模式 before-group / after-group 顶层意图）时，drop indicator 染红、组头加红色 outline、光标变 `not-allowed`，让"为什么释放不会移动"在松手前就显性化。

### Changed
- **设置页信息架构收敛 (Settings Information Architecture Consolidation)**: 将设置页的导出、导入和版本历史合并到“备份与恢复”，将反馈与诊断信息合并到“帮助与反馈”，保存状态移动到标题栏，并仅在导入预览有效时显示“应用导入”按钮；来源修复仅在检测到匹配问题时独立突出显示。
- **命令快捷键重复触发 (Command Shortcut Repeat Toggle)**: 用户自定义命令快捷键再次触发同一搜索、快速视图或 modal 类命令时，会收起搜索、退出当前快速视图或关闭已打开的对应面板。
- **按钮扫光动效关闭 (Button Sweep Hover Motion Disabled)**: 关闭 `.sp-button` 的 shimmer 扫光和 `.sp-glare-hover` 的 glare 扫光伪元素，保留普通 hover 背景、边框和缩放反馈。
- **启动弹窗版本分流 (Startup Modal Version Routing)**: 首次使用的新用户只显示欢迎弹窗并标记当前版本更新说明已处理，已有本地插件数据的老用户升级后按 manifest 版本显示一次 What's New 弹窗。
- **深拷贝改用 structuredClone (Deep Clone via structuredClone)**: **影响**: 对扩展使用无可感知变化;深拷贝改用原生 structuredClone。 将 `src/background/index.js` 的 `cloneSerializableData`、`src/content/content-developer-logger.js` 的 `getDeveloperLogs` 与 `src/content/content-state-reconcile.js` 的 `applySourceRemapsToSnapshot` 中裸 `JSON.parse(JSON.stringify)` 替换为 `structuredClone` 优先并保留 JSON 回退，与 `content-persistence.js`、`content/index.js` 已有模式一致。
- **项目目录索引同步 (Project Directory Index Sync)**: **影响**: 对扩展使用无可感知变化;同步项目目录索引文档。 更新 `docs/PROJECT_DIRECTORY.md` 目录树、Runtime 加载树和功能域树，补齐 `content-state-apply`、`content-undo-history`、`content-import-export`、`content-modal-{welcome,whats-new,tag-filter,move,command-palette,tag,settings}` 与 `content-toast` 等模块条目。
- **快照签名 helper 抽离 (Snapshot Signature Helper Extraction)**: **影响**: 对扩展使用无可感知变化;快照签名逻辑抽成独立 helper 的纯重构。 将 `content-persistence.js` 中纯粹的快照比较与配额识别逻辑（`isStorageQuotaError`、`getStorageMetadataFromResponse/Result`、`getSnapshotSaveRevision`、`isStaleStateWrite`、`getStableComparablePersistableValue`、`getPersistableSnapshotSignature`、`arePersistableSnapshotsEquivalent`）抽离到新模块 `content-snapshot-signature.js`；persistence 在 factory 启动时从 `globalThis.NSM_CREATE_CONTENT_SNAPSHOT_SIGNATURE` 取 helpers，公共返回的字段不变，`src/content/index.js` 与 `src/background/index.js` 调用方无需调整。同时在 manifest/load-content-module/test harness 中登记新模块，并补 `tests/content/content-snapshot-signature.test.js` 单测覆盖所有 helper。
- **原生标签检测 helper 抽离 (Native Label Detector Helper Extraction)**: **影响**: 对扩展使用无可感知变化;原生标签检测逻辑抽成独立 helper 的纯重构。 将 `content-source-sync.js` 中的 9 个原生标签/视图切换识别 RegExp 常量与 8 个检测函数（`cleanAccessibleLabelTitle`、`collapseRepeatedNativeLabelTitle`、`cleanNativeLabelTitleCandidate`、`getComparableLabelText`、`getComparableNativeLabelTitle`、`isLikelyNativeLabelTitle`、`isNativeLabelEntryPointControl`、`isNativeSourceViewSwitchControl`）抽离到新模块 `content-native-label-detector.js`；source-sync 在工厂启动时把 `getAttributeValue`/`getElementTextSignal` 作为 deps 传入，再 destructure 拿回常量与函数，所有调用点行为不变。`content-source-sync.js` 从 2708 行降到 2635 行；并新增 `tests/content/content-native-label-detector.test.js` 单测覆盖全部 8 个函数与常量导出。
- **原生 checkbox 同步 helper 抽离 (Native Checkbox Sync Helper Extraction)**: **影响**: 对扩展使用无可感知变化;原生 checkbox 同步逻辑抽成独立 helper 的纯重构。 将 `content-tree-interactions.js` 中的 `getNativeControlAttribute`、`getNativeCheckboxState`、`shouldToggleNativeCheckbox`、`resolveDetachedRowEntry` 抽离到新模块 `content-native-checkbox-sync.js`，工厂接受 `findFreshCheckbox`/`resolveFreshRowEntry` 作为可选 deps；tree-interactions 在工厂启动时调用并 destructure，所有调用点行为不变。`content-tree-interactions.js` 从 1146 行降到 1117 行；并新增 `tests/content/content-native-checkbox-sync.test.js` 单测覆盖 4 个函数的常规分支与回退路径，同时让 tree 测试套件在直接 require tree-interactions 前先加载新模块。
- **状态结构修复 helper 抽离 (State Structure Repair Helper Extraction)**: **影响**: 对扩展使用无可感知变化;状态结构修复逻辑抽成独立 helper 的纯重构。 将 `content-persistence.js` 中的灾难恢复集群 `collectSnapshotGroupedSourceKeys`、`getSnapshotGroupInfo`、`isCompatibleStructuralRepairCandidate`、`createStructurallyRepairedState`、`findStructuralRepairCandidate` 抽离到新模块 `content-state-repair.js`，工厂接受 `cloneSerializableData`/`hasRestorableStateSnapshot`/`getMapLikeEntries`/`normalizeStateHistoryEntries`/`getSnapshotSaveRevision` 作为 deps（带合理默认值），persistence 在工厂启动时 destructure 五个函数，所有 `pickPreferredStoredState` 内部调用点行为不变。`content-persistence.js` 从 1830 行降到 1704 行；并新增 `tests/content/content-state-repair.test.js` 单测覆盖 5 个函数（含 cyclic 防御、title-overlap 回退、source-mismatch 拒绝、save-revision tie-break）。
- **ESLint 静态检查接入 (ESLint Static Analysis Integration)**: **影响**: 对扩展使用无可感知变化;接入 ESLint 静态检查。 新增 `eslint` 9.39 dev 依赖与 `eslint.config.js` flat config；按 src/tests/scripts 分别配 globals（NSM_* 全部工厂、utils helper、Chrome MV3、Jest、Node），启用 `no-undef`、`no-useless-escape`、禁止 `innerHTML` 赋值（与 AGENTS.md 一致），并在 tests/smoke 子作用域里放宽 `no-restricted-syntax`。`package.json` 新增 `lint` 与 `verify:full` 编排 `npm run lint`；GitHub Actions CI 在 `npm run test:unit` 之前增加 `npm run lint` 步骤。当前 baseline：0 errors、89 warnings（多为 src/ 中可能未使用的 helper，留作后续清理）。
- **content-drag-multi 模块抽离 (Content Drag Multi Module Extraction)**: **影响**: 对扩展使用无可感知变化;多源拖拽逻辑抽成独立模块的纯重构。 新增 `src/content/content-drag-multi.js`，把多源选择解析、自定义 ghost、批量 drop 应用、边缘速度计算和 RAF 滚动控制器集中到单一 factory；`content-tree-interactions.js` 在 dragstart/dragover/dragleave/drop/dragend 五个点接入新模块。
- **项目目录索引补齐 (Project Directory Index Patch)**: 在 `docs/PROJECT_DIRECTORY.md` 的 Runtime 加载树、功能域树（分组树/拖拽/批量模式）和测试树 smoke 列表中补齐 `content-drag-multi.js` 与 `tests/smoke/batch-drag.smoke.spec.js` 入口，与新加的批量拖拽 + 边缘自动滚动模块保持同步。
- **悬停展开可逆 (Reversible Hover-Expand)**: 拖拽悬停展开的折叠组现在对称可逆 — 指针离开该组的子树 600ms 后自动收回；拖动指针回到该组或其后代之内可取消收回计时器、保持展开。drop 落入该组的子树永久保持展开；drop 在子树外或拖动结束时未落进去则立即收回。手动通过 chevron 展开的分组不受此机制影响。

### Fixed
- **lifecycle 测试未定义 listSource 修复 (Lifecycle Test Undefined listSource Fix)**: **影响**: 对扩展使用无可感知变化;修复一个生命周期测试的问题。 修复 `tests/content/content-lifecycle.test.js` 第 650 行 "clicks the native label_auto header entry point when switching from plugin list display to label view" 测试缺失 `listSource` 局部变量（之前依赖一个 `eslint-disable-next-line no-undef` 临时绕过）；按照同文件其他类似测试的写法补回 `createMockSourceRow` 调用，并删除 eslint-disable 注释。测试已恢复完整定义，行为不变。
- **抽取造成的死引用清理 (Dead Destructure Cleanup From Recent Extractions)**: **影响**: 对扩展使用无可感知变化;清理重构后遗留的死引用。 删除 `content-source-sync.js`、`content-persistence.js`、`content-tree-interactions.js` 在 destructure 工厂返回值时引入但实际未在该文件内使用的 12 个名字（source-sync 8 个 PATTERN/helper、persistence 中 `getStableComparablePersistableValue`/`getSnapshotGroupInfo`/`isCompatibleStructuralRepairCandidate`、tree-interactions 中 `getNativeControlAttribute`）。这些 helper 仍在各自所属模块内部使用，对外部调用方无影响。ESLint warnings 从 89 降到 77。
- **ESLint warnings 清零 (ESLint Warnings Zeroed Out)**: **影响**: 对扩展使用无可感知变化;清零所有 ESLint 警告。 删除 `src/content/index.js` 中 63 行未使用的 destructured 名字（多个 module factory 返回值的 over-destructure）、给一处 rest pattern 的 explicit-exclude 加 `_` 前缀、丢弃一处 `getSourceViewInfo` 的死赋值（保留 side effect）；删除 `content-import-export.js`/`content-modal-tag.js`/`content-modals.js`/`content-persistence.js`/`content-source-sync.js`/`content-tree-interactions.js` 中预先存在的未用 dep wrapper 与级联死代码（`applyPersistableSnapshotToRuntime`、`isSourcePanelRenderable`、`getMessage`、`getParentMap`、`getKeyByElement`、`setPendingInitialLoadedState`、`setSourceTagIds`、`renderTagModal`、`getTagColorRgba`/`getTagColorRgb`、`getSourceTagsById`）；修正 `tests/content/content-modals-tags.test.js` 两处 `\[` 字符类内不必要转义。本次 ESLint baseline 从 77 warnings 降到 0 warnings、0 errors，全部 754 个单元测试继续通过。
- **偏好与恢复点保存失败反馈 (Preference and Restore Point Save Failure Feedback)**: 修复语言、历史保留数量等偏好保存失败时设置页误报成功的问题，并让手动恢复点创建在 history 写入失败时显示失败结果。
- **命令面板过滤与最近来源时间戳 (Command Palette Filtering and Recent Source Timestamps)**: 修复命令面板输入内容不会过滤命令的问题，并在无原生 DOM 行的恢复路径中保留来源 `addedAt`，避免 Recent 视图时间戳被后续保存抹掉。
- **发布包图标保留校验 (Release Icon Asset Guard)**: 补充发布包图标资源校验，确保插件图标资源继续进入 release zip。
- **全部视图边界遮挡 (All View Edge Clipping)**: 修复 All 快速视图下快速筛选按钮被渲染成裁切括号状、以及来源列表最后一行贴近底部边界时可能被面板边缘、拖拽手柄或 NotebookLM 原生控件截断的问题。
- **空快速视图栏隐藏 (Empty Quick View Rail Hiding)**: 修复用户在设置中隐藏所有 quick view 按钮后，来源面板仍保留空白按钮栏区域的问题。
- **content-test-harness 全局列表同步 (Content Test Harness Globals Sync)**: **影响**: 对扩展使用无可感知变化;同步测试 harness 的全局列表。 在 `tests/helpers/content-test-harness.js` 的 `CONTENT_HELPER_GLOBALS` 数组中补齐 `NSM_CREATE_CONTENT_DRAG_MULTI`，按 manifest 加载顺序放在 `NSM_CREATE_CONTENT_NATIVE_CHECKBOX_SYNC` 之后；之前 Task 1 在登记新模块时只更新了 `load-content-module.js`，遗漏了 AGENTS.md 要求一同更新的 harness 文件，会导致使用该 harness 的测试之间 global 残留。修复后 lint 0/0、890 个单元测试继续通过。
- **toggleGroupCollapse 展开动画 (toggleGroupCollapse Expand Animation)**: **影响**: 拖拽悬停展开文件夹时改为平滑展开动画,不再瞬间弹开。 修复 `toggleGroupCollapse` 在 expand 分支没有显式起始帧 + 强制 reflow，导致拖拽 hover-expand 路径（dragover 同步栈，没有事件循环间隙）展开时 `height` transition 不触发、组瞬间弹开的问题。改为先 `style.height = '0px'` 显式锁定起始帧，再 `classList.remove('collapsed')`，再读 `offsetHeight` 强制 layout 提交 0 帧，最后 `style.height = scrollHeight` 触发动画。点击 chevron 路径原本"碰巧"工作，现在与 hover-expand 路径行为一致、动画稳定。

### Security
- **本地会话文件忽略 (Local Session File Ignore)**: 将根目录 `cookies.json` 加入 `.gitignore`，避免小红书或其他本地登录工具生成的会话文件被误提交。

## [2026-05-17] [2.7.4]

### Added
- **首次欢迎弹窗 (First-Run Welcome Modal)**: 在用户首次进入 NotebookLM 并成功加载来源管理器后显示一次欢迎弹窗，提供功能简介和 Chrome Web Store 反馈入口，并支持浅色/深色模式及 English、Español、简体中文三语言文案。
- **欢迎弹窗测试入口 (Welcome Modal Test Entry)**: 在设置页开发者功能区新增“测试欢迎弹窗”按钮，方便重复预览首次欢迎弹窗而无需清理偏好状态。
- **来源视图状态记忆 (Source View State Memory)**: 每个 notebook 会记住上次使用的来源列表/标签视图，并在下次打开时自动恢复对应视图。

### Changed
- **反馈入口文案精简 (Feedback Entry Copy Simplification)**: 移除设置页反馈区里 Chrome Web Store 说明小字，仅保留反馈标题和打开反馈入口按钮。
- **欢迎反馈入口轻量化 (Welcome Feedback Entry Simplification)**: 将首次欢迎弹窗底部反馈区域从卡片和按钮改为小字提示与文字链接，降低与功能介绍卡片的视觉重复。
- **开发者模式解锁记忆 (Developer Mode Unlock Memory)**: 当 Developer Mode 已开启时，设置页会直接显示开发者功能区，刷新或重新进入 notebook 后无需再次输入开发者密码。
- **保存状态横向排列 (Horizontal Save Status Layout)**: 将设置页“保存状态”标题和状态徽标改为同一行横向排列，避免状态内容在宽面板中垂直堆叠。
- **开发者功能隐藏入口 (Developer Features Hidden Entry)**: 设置页不再直接展示开发者模式、日志和欢迎弹窗测试按钮，改为底部“开发者功能”小入口，输入开发者密码 `developer_mode` 后才显示这些工具。
- **UI 规范同步 (UI Guidelines Alignment)**: 将 `UI_GUIDELINES.md` 同步为当前实现事实，覆盖 popup 宽度与暗色主题、source row grid 布局、长来源标题换行策略、motion token 三曲线体系和最新 UI helper 文件入口，并在项目目录索引中补充 UI 规范排障入口。
- **项目文档一致性 (Project Documentation Consistency)**: **影响**: 对扩展使用无可感知变化;统一项目文档与实际实现一致。 统一项目目录中的 state backup key 为实际使用的 `sourcesPlusState_<projectId>__backup`，更新 README smoke 覆盖说明，补齐 release checklist 的 `package-lock.json`、README badge 和 changelog 发布步骤，并把 `UI_GUIDELINES.md` 纳入 UI 变更入口规则。
- **Agent 项目规则强化 (Agent Project Rules Hardening)**: 在 `AGENTS.md` 中固定启动检查、content helper 装配、验证矩阵、NotebookLM 原生自动化安全、开发者日志脱敏、headless smoke 默认值和只读检查无需 changelog 的规则，并同步更新目录索引。
- **Content Helper 边界拆分 (Content Helper Boundary Split)**: **影响**: 对扩展使用无可感知变化;内容脚本 helper 边界拆分的纯重构。 新增 toast/status、diagnostics、source view switch、native label import、partial sync guard、source action menu 和 modal focus helper/controller，并补充对应 focused 单测；`index.js`、source sync、source actions 和 modal 模块继续保留原入口行为。
- **Modal 子模块边界 (Modal Helper Boundary)**: 新增原生标签导入 modal helper，把导入 preview 节点生成逻辑从 `content-modals.js` 抽离，保留原 modal facade 和交互入口不变。
- **Source Sync 子模块边界 (Source Sync Helper Boundaries)**: 新增来源列表扫描、原生标签扫描和原生标签导入 helper，先抽离 checkbox 状态读取、标签头数量解析和导入预览完整性判断，为后续继续拆分 `content-source-sync.js` 建立可测试边界。
- **Content 消息路由边界 (Content Message Router Boundary)**: 新增 content message router helper，把 popup/background 到 content 的消息分发机制从总入口抽离，并保持 `GET_MANAGER_STATUS`、`FOCUS_MANAGER`、`SWITCH_SOURCE_VIEW`、启用和禁用响应形状不变。
- **Content Runtime 状态边界 (Content Runtime State Boundary)**: 新增 content runtime state helper，先把 `runtimeContext` getter/setter 绑定机制从 content 总入口中抽离出来，后续可继续收敛运行时状态字段而不改变现有业务行为。
- **架构契约文档 (Architecture Contract Docs)**: 新增 storage schema 和 message contract 文档，明确状态字段、迁移边界、消息类型、sender 校验、key 前缀和错误码，作为后续拆分 content runtime 与 source sync 的维护契约。
- **更新日志规范化 (Changelog Standardization)**: 统一历史条目的分类标题、双语标题格式和非营销化描述，并补充 `Unreleased` 写入模板，方便后续人工和 agent 按同一规则维护。
- **Agent 更新日志流程 (Agent Changelog Workflow)**: 在 `AGENTS.md` 中明确后续 agent 写入 changelog 的步骤：默认写入 `Unreleased`、只在发布时创建正式版本段，并同步检查项目目录索引。
- **目录索引维护规则 (Directory Guide Maintenance Rule)**: 新增根目录 `AGENTS.md`，明确后续每次代码、测试、文档、配置、打包或工作流变更都必须同步检查并更新 `docs/PROJECT_DIRECTORY.md`，同时在目录索引中标出该规则入口。
- **项目目录索引 (Project Directory Guide)**: 新增 `docs/PROJECT_DIRECTORY.md`，用树状结构按目录结构、运行入口、功能域、存储 key、测试入口、发布流程和 agent 快速定位路径整理仓库维护地图，并标明清理后可重新生成的 ignored 输出目录，方便后续人工或 AI agent 快速定位相关代码与验证路径。

### Fixed
- **原生视图切换前状态同步 (Native View Switch Pre-Sync)**: 修复在 NotebookLM 原生标签视图中修改折叠标签组勾选状态后，直接点击原生列表视图按钮会丢失最新勾选状态的问题。
- **原生标签菜单切换防护 (Native Label Menu Switch Guard)**: **影响**: 在 NotebookLM 列表视图与标签视图之间切换更可靠,不再误点来源行或菜单项。 修复 List View 切到 Label View 时真实 header `label_auto` 入口被误判为来源行按钮、以及 Label View 切回 List View 时 NotebookLM 只在 `label_auto` 菜单中提供返回入口的问题；现在允许明确的 header `label_auto` 进入标签视图，并仅通过 `label_auto` -> `Return to list view` 返回列表，同时继续避免误点来源行或普通菜单项。
- **来源指纹稳定性 (Source Fingerprint Stability)**: 修复来源 hydrate 后新增与标题重复的 ARIA label 会改变 stable-token 来源 fingerprint、导致硬刷新后产生无意义额外保存的问题。
- **设置弹窗字体继承修复 (Settings Modal Font Inheritance Fix)**: 为内容脚本 modal 外壳固定与来源管理器一致的系统字体栈，避免设置和欢迎弹窗继承 NotebookLM 页面字体导致字体不匹配。
- **原生标签导入确认 (Native Label Import Confirmation)**: 修复只读导入预览已列出 NotebookLM 原生标签，但确认导入时因来源尚未进入插件状态而导入结果为 0 个分组 / 0 个来源的问题；现在仅在确认阶段补齐 preview 中的来源记录。

### Removed
- **本地产物清理 (Local Artifact Cleanup)**: 清理 `.DS_Store`、`.Rhistory`、旧计划文件、旧 zip 归档以及可重新生成的 `release/`、`output/` 目录，减少维护时的干扰项。

## [2026-05-16] [2.7.2]

### Added
- **开发者模式日志 (Developer Mode Logging)**: 在设置页新增开发者模式开关，开启后记录脱敏的状态变更、原生操作、持久化、导入导出、视图切换和错误日志，并支持复制、下载和清空开发者日志；新增 `docs/DEVELOPER_LOGGING.md` 作为后续日志写作规范。
- **开发回归清单 (Development Smoke Checklist)**: 在 README 中新增面向扩展开发的手工冒烟检查步骤，覆盖 manager 挂载、图标回归、popup 上下文分支、NotebookLM 路由切换，以及分组、拖拽和批量操作等核心交互。

### Changed
- **无弹窗 Smoke 测试默认值 (Headless Smoke Test Default)**: 本地 `npm run test:smoke` 现在默认以 headless 模式运行，避免 Playwright 浏览器窗口抢占桌面；需要交互调试时可显式使用 `PLAYWRIGHT_HEADLESS=false npm run test:smoke`。
- **来源描述符辅助逻辑抽离 (Source Descriptor Helper Extraction)**: 将 source descriptor、稳定 token、图标 URL 解析等纯逻辑从 `content/index.js` 抽离到独立 helper，保持现有全局 helper 装配方式不变，降低内容脚本单文件耦合度。
- **当前 Popup 和 UI 改动收口 (Popup and UI Consolidation)**: 保留当前 popup 视觉结构和内容面板样式 token 的整理结果，并补上结构契约测试，确保关键 DOM id 与 launcher 行为没有因为 UI 调整而回归。

### Fixed
- **安全加固 (Security Hardening)**: 阻断从 NotebookLM DOM/CSS 提取的任意第三方来源图标 URL，收紧 content script 在 background 拒绝或通信失败时的 direct storage fallback，并在原生删除/重命名前增加 fresh row 身份校验与删除确认弹窗歧义防护。
- **NotebookLM 标签/列表切换状态同步 (Native Label/List State Sync)**: 修复在原生标签视图中折叠标签组后修改组头复选框，再切回列表视图时插件仍恢复旧勾选状态的问题；source scan 与原生 change 事件统一读取 `checked`、`aria-checked` 和 checkbox role，并避免原生标签组盲目匹配普通同名插件文件夹。
- **原生删除结果判定 (Native Delete Confirmation)**: 修复 NotebookLM 已接受删除请求但来源行尚未从 DOM 立即移除时，插件误报“删除失败，重试”的问题；确认弹窗关闭后会按删除请求已被接受处理，并先从插件 manager 状态中隐藏该来源。
- **原生删除弹窗识别补强 (Native Delete Dialog Detection)**: 补充识别 NotebookLM 当前 Material/CDK 删除确认弹窗的 overlay pane 与 dialog surface 容器，并支持 `[role="button"]` 形式的确认按钮，避免原生弹窗已经显示但插件仍误报未找到确认弹窗。
- **批量模式来源标题布局 (Batch Mode Source Title Layout)**: 修复进入批量操作后隐藏三点按钮导致来源标题掉入操作列、长标题逐字换行的问题；批量模式现在保留不可交互的操作列占位。
- **批量选中动画一致性 (Batch Selection Motion Consistency)**: 移除批量选中来源行的自定义 pop 动画，让选中和取消选中都使用默认状态切换，避免两种操作的动效不一致。
- **批量操作条按钮显示 (Batch Action Bar Labels)**: 修复批量操作条里“取消”按钮被挤压时可能显示不完整的问题，并取消非删除操作后的重复数量显示，仅保留删除按钮显示当前选中数量。
- **来源导入中状态同步 (Importing Source State Sync)**: 修复 NotebookLM 添加来源时临时 loading 行被同步保护逻辑跳过的问题；现在会保留旧来源并合并新的 loading 行，识别 raw URL 临时标题、progress bar、spinner、状态文案和 `aria-busy` 等 loading 信号。
- **失败来源状态显示 (Failed Source State Display)**: 修复原生 NotebookLM 已标记失败但来源行仍带 checkbox 时，插件没有显示失败样式的问题；失败来源现在会被标记为不可用，并在 manager 中显示失败图标、失败提示和禁用控件。
- **失败来源删除入口 (Failed Source Delete Action)**: 修复失败来源被 disabled 状态拦截后无法打开三点菜单的问题；现在失败来源的菜单只显示“删除失败来源”，并允许继续走 NotebookLM 原生删除确认流程。
- **原生标签导入展开识别 (Native Label Import Expansion Detection)**: 修复 NotebookLM 标签组只通过 chevron 图标或 Material `treeitem` 表示折叠时，点击“导入 NotebookLM 分组”仍提示没有可导入分组的问题；导入扫描也会排除 Material 标签头里的图标文本和底部自动标签入口。
- **折叠标签组导入预览 (Collapsed Native Label Import Preview)**: 修复 NotebookLM 原生标签组未全部展开时，导入预览依赖可见来源行导致可导入文件夹为空或不完整的问题；现在优先读取折叠组中仍存在的隐藏来源行，必要时才使用自动展开兜底。
- **原生标签导入只读预览 (Read-Only Native Label Import Preview)**: 将“导入 NotebookLM 分组”预览改为直接从当前 DOM 只读生成，不再先写入插件来源状态；隐藏来源行只允许来自原生折叠标签组内容区，并且导入确认只复用带 `nativeLabelTitle` 的原生导入分组。
- **分组树交互健壮性 (Group Tree Interaction Hardening)**: 修复同一毫秒连续新建分组时 `Date.now()` 生成相同 ID 导致分组覆盖的问题，同时避免父分组已过期时创建不可见孤儿分组，以及来源行 DOM 已过期但来源记录不存在时点击行触发运行时异常。
- **历史/导入分组数据容错 (Persisted Group Shape Guarding)**: 修复历史数据或导入数据中的分组缺少 `children` 数组时，移除分组、状态重映射、构建父级索引或渲染列表可能抛错的问题；现在这些路径会按空子列表处理。
- **标签状态更新容错 (Tag State Update Guarding)**: 修复只更新标签名称时会意外清空原有颜色的问题，并在旧状态缺少 `tagOrder` 时自动初始化，避免创建或排序标签时报错。
- **移动到文件夹容错 (Move-to-Folder Guarding)**: 修复目标文件夹缺少 `children` 数组时移动来源会抛错的问题；现在移动前会初始化目标子列表并继续正常保存。
- **来源图标提取性能收敛 (Source Icon Extraction Hardening)**: 将来源图标发现逻辑改为显式候选优先、受限回退扫描，避免在 NotebookLM 复杂 DOM 上做高成本整行深搜与无差别样式读取。
- **来源图标误判防护 (Source Icon False-Positive Guarding)**: 保留并强化对原生 checkbox、三点菜单按钮和交互菜单祖先路径的排除，修复装饰性图标被错认成来源图标的风险，同时继续支持可点击 source row、本地化未命名来源兜底和 shadow-root 图标场景。

## [2026-03-17] [2.6.1]

### Changed
- **版本同步与重新打包 (Version Sync and Repackage)**: 将清单、包信息、README 徽章及发布压缩包统一升级为 2.6.1，并重新生成对应 release zip。
- **发布命名统一 (Release Name Alignment)**: 将发布包与项目包名统一为 `notebooklm-source-management`，不再使用 `notebooklm-source-plus` 命名。

### Fixed
- **Launcher 路径补强 (Launcher Route Handling)**: 细化工具栏启动器的状态分支，修复用户已在 NotebookLM 首页但尚未进入具体笔记本时点击后没有明确反馈的问题。
- **NotebookLM 首页反馈 (NotebookLM Home Feedback)**: 启动器会优先切换到已打开的 notebook 标签页；若当前页已是唯一的 NotebookLM 首页标签页，则新开一个 NotebookLM 标签页。
- **笔记本路由挂载 (Notebook Route Mounting)**: 针对 NotebookLM 单页应用路由切换，新增进入或切换 notebook 时的一次性自动刷新，确保扩展正确加载并避免来源列表异常消失。

## [2026-03-17] [2.6.0]

### Added
- **浏览器工具栏启动器 (Toolbar Launcher)**: 为扩展新增独立的浏览器工具栏 popup 入口；固定到工具栏后点击图标会进入扩展自己的状态页与启动器界面。
- **页面内聚焦跳转 (In-Page Focus Jump)**: 新增 popup 到 content script 的通信链路，用户可从工具栏一键跳转并高亮 NotebookLM 页面中的来源管理器。

### Changed
- **入口说明重构 (Entry-Point Clarity)**: 明确区分“工具栏图标是启动器”与“真正功能运行在 NotebookLM 来源面板内”的产品模型，并补充多语言提示文案与 README 安装说明。
- **版本同步 (Version Sync)**: 将源码清单与包信息统一升级至 2.6.0，避免发布产物与仓库源码版本长期脱节。

## [2026-03-15] [2.5.0]

### Changed
- **版本跟进更新 (Version Update)**: 将清单文件与核心配置升级至 2.5.0，并重新构建用于发布的 zip 压缩包。

## [2026-03-08] [2.4.1]

### Changed
- **批量删除队列优化 (Batch Delete Queue Optimization)**: 调整批量删除的异步队列调度和 DOM 状态同步，减少高并发删除期间 observer 脱节和列表刷新不准的问题。

## [2026-03-07] [2.4.0]

### Changed
- **核心稳定性提升 (Core Stability Update)**: 重构底层同步和防御性逻辑，增强单页应用页面在高频异步操作和复杂 DOM 状态下的运行稳定性。

### Removed
- **冗余文件清理 (Redundant File Cleanup)**: 移除历史遗留的开发辅助教程与计划文档，包括 `PLAN.md`、`GITHUB_PUBLISH_TUTORIAL.md`、`GIT_COMMANDS_GUIDE.md` 和 `TERMINAL_COMMANDS_GUIDE.md`。

## [2026-03-05] [2.3.0]

### Added
- **未分组文件移动入口 (Move to Folder Modal)**: 为未分组来源新增文件夹快捷移动入口，支持选择目标分组、移动来源，并补充相关多语言文案。

## [2026-03-05] [2.2.0]

### Added
- **未分组文件移动面板 (Move to Folder Modal)**: 为未分组来源新增独立的“加入文件夹”入口，避免依赖 NotebookLM 原生三点菜单注入。
- **自适应退避心跳 (Adaptive Backoff Heartbeat)**: 将 DOM 同步检测从固定高频查询改为自适应退避轮询，在界面闲置、DOM 无变动时降低健康检查频率。
- **统一交互动效 (Unified Interaction Motion)**: 统一弹窗、折叠面板和按钮微交互使用的缓动参数，减少不同交互之间的动画不一致。

### Changed
- **删除确认寻址增强 (Delete Confirmation Targeting)**: 增强批量删除在查找 NotebookLM 原生删除对话框时的寻址逻辑，减少文案变化导致确认按钮定位失败的风险。

### Fixed
- **MutationObserver 性能收敛 (MutationObserver Throttle)**: 移除对 `characterData` 的无意义监听，减少 spinner 和进度条变化引发的无效同步工作。

## [2026-03-01] [2.1.1]

### Added
- **后台持久化架构 (Background Persistence Architecture)**: 引入 background service worker，将 `chrome.storage` 相关状态持久化逻辑迁移到后台，并建立 message passing 通信机制。
- **批量删除入口 (Batch Delete Action)**: 在界面顶部栏新增批量删除动作按钮，支持进入多选删除模式并串行触发 NotebookLM 原生删除流程。
- **加载状态显示 (Loading State Display)**: 增加来源导入中的 loading 状态识别和渲染，在 NotebookLM 仍在解析来源时显示加载环和状态文本。
- **全局毛玻璃样式 (Global Glassmorphism)**: 调整 manager 控制栏和部分挂载容器的视觉样式，使其与当前插件界面风格保持一致。
- **文件夹入场动画 (Folder Entry Animation)**: 为新建分组添加入场动画，减少分组 DOM 插入时的突兀闪现。
- **国际化多语言支持 (Internationalization)**: 将硬编码 UI 文案迁移到 Chrome `chrome.i18n`，并添加英文和简体中文文案配置。
- **维护文档补充 (Maintenance Documents)**: 新增 Git、终端命令和 GitHub 发布相关的维护文档。
- **失败来源高亮 (Failed Source Highlighting)**: 检测并高亮显示导入失败且无法选中的来源文件。

### Changed
- **视觉风格调整 (Visual Style Update)**: 调整全局动画参数和弹出菜单、挂载容器的圆角比例，使现有界面视觉保持一致。
- **代码结构整理 (Code Structure Cleanup)**: 整理渲染和交互代码结构，降低后续维护难度。

### Fixed
- **XSS 安全修复 (XSS Rendering Fix)**: 移除 `content.js` 中对用户可控内容使用 `innerHTML` 的路径，改用 `document.createElement` 方式渲染。
- **扩展上下文失效防御 (Invalidated Context Guard)**: 修复扩展 reload 后旧标签页继续调用 `chrome.runtime.sendMessage` 可能抛出 `Extension context invalidated` 的问题。
- **CSS 选择器抗变更 (CSS Selector Resilience)**: 将部分 DOM 查询优先改为 `data-testid` 和语义标记，降低宿主页面混淆类名变化带来的失效风险。
- **来源列表自动刷新 (Source List Auto Refresh)**: 修复添加新来源后插件面板不同步的问题，通过更稳定的 DOM 监听锚点和轻量同步心跳处理列表容器重建。
- **勾选动画重播 (Checkbox Animation Replay)**: 修复 DOM 重绘时所有已选中来源同时重播选中动画的问题，将动画触发限制为用户主动点击。
- **原生弹窗闪烁 (Native Dialog Flicker)**: 在批量删除自动化期间临时隐藏 NotebookLM 原生确认弹窗，减少连续删除时的视觉闪烁。
- **右侧滚动条异常 (Unexpected Right Scrollbar)**: 修复整个界面右侧有时会意外出现滚动条的问题。
- **菜单定位异常 (Menu Positioning)**: 修复插件菜单在部分布局下定位不正确的问题。
